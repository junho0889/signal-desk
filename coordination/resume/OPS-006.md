# Resume OPS-006

## Task
- id: OPS-006
- role: signal-desk-ops
- branch: codex/ops-006
- worktree: E:\source\signal-desk-worktrees\ops-006
- last updated: 2026-03-08

## Current State
- ops docs are updated with the required collector local test-db workflow
- task is ready for orchestrator review, but runtime execution is blocked by missing collector assets

## Last Completed
- defined exact boot, reset, fixture ingest, restart, and SQL inspection commands in the owned ops docs
- aligned the runbook to current metadata-quality, storage, and intake contract terms
- added the missing OPS-006 dispatch file to this worktree

## Next Exact Step
- wait for collector implementation assets under `infra/collector/` and `services/collector/`, then rerun the documented OPS-006 commands and capture real output

## Open Blockers
- `infra/collector/docker-compose.yml` does not exist yet in this worktree or the source repo snapshot
- collector service implementation is not present yet, so the fixture-run command surface is frozen as an ops requirement rather than a tested runtime
- `COL-003`, `BE-008`, and `BE-009` handoffs are still pending

## Verification Status
- consistency review passed against collector runtime, metadata-quality, storage, and intake docs
- `git diff --check` passed apart from line-ending warnings on edited Markdown files
- runtime verification is blocked by missing collector assets, not by missing ops guidance

## Files In Progress
- none

## Last Commit And Push
- commit: `8f038b4` (`OPS-006 docs: define collector test-db workflow`)
- push: `git push -u origin codex/ops-006` succeeded on 2026-03-08

## Notes For Next Session
- do not relax the separate collector project boundary; keep `signaldesk-collector-test` separate from the main app stack
- if downstream lanes change table names from `collector_spool_items`, `raw_items`, or `raw_item_quality_states`, either add compatibility views or update OPS-006 before QA-007
- no Windows system settings should be required for the local collector smoke path
