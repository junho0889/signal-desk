# Resume OPS-006

## Task
- id: OPS-006
- role: signal-desk-ops
- branch: codex/ops-006
- worktree: E:\source\signal-desk-worktrees\ops-006
- last updated: 2026-03-08

## Current State
- ops docs are updated with the exact collector local test-db workflow from reviewable `COL-003`
- the minimal collector promotion path is frozen for Raspberry Pi `192.168.0.33`
- task is ready for orchestrator review; runtime execution in this worktree still depends on collector assets being integrated here

## Last Completed
- replaced the earlier speculative collector workflow with the concrete `COL-003` compose services, env keys, helper script, and SQL query surface
- added the minimal Pi deployment path for `192.168.0.33` without changing the local smoke contract
- prepared the handoff to point future runtime verification at the exact frozen commands

## Next Exact Step
- if collector assets merge into this worktree, run the documented local smoke path directly here and capture runtime output
- otherwise, use the same command surface against the next integrated collector worktree and then against Raspberry Pi `192.168.0.33`

## Open Blockers
- `infra/collector/docker-compose.yml` and `services/collector/` still do not exist in the OPS-006 worktree itself
- Raspberry Pi deployment still needs a later session with Docker access on `192.168.0.33` and frozen central-target env values

## Verification Status
- consistency review passed against the reviewable `COL-003` collector assets and handoff
- `git diff --check` passed apart from line-ending warnings on edited Markdown files
- runtime verification is blocked by worktree content, not by missing ops guidance

## Files In Progress
- `docs/ops/local-docker-stack.md`
- `docs/ops/deploy-runbook.md`
- `docs/ops/quality-gates.md`
- `coordination/handoffs/OPS-006.md`
- `coordination/resume/OPS-006.md`

## Last Commit And Push
- commit: `f7f4bba` (`OPS-006 chore: record collector test-db handoff`)
- push: `git push -u origin codex/ops-006` succeeded on 2026-03-08

## Notes For Next Session
- do not relax the separate collector project boundary between `infra/local` and `infra/collector`
- keep the Pi path command-compatible with local smoke; change only node identity, host-local env, and future central-target settings
- no Windows system settings should be required for the local collector smoke path
