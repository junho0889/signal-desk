# Resume COL-006

## Task
- id: COL-006
- role: signal-desk-collector
- branch: codex/col-006
- worktree: E:\source\signal-desk-worktrees\collector-006
- last updated: 2026-03-09

## Current State
- Done. Raspberry Pi deploy and remote ingest smoke completed successfully.

## Last Completed
- SSH login to `admin@192.168.0.33` succeeded via `signaldesk-pi`.
- remote Docker checks succeeded (`docker 28.4.0`, `docker compose v2.39.2`).
- synced collector assets to `/home/admin/signal-desk-collector`.
- cleared stale container-name conflict and rebooted collector compose stack.
- executed remote fixture ingest and spool SQL evidence queries.
- verified `collector-db` is running healthy on Pi.

## Next Exact Step
- hand off to `COL-007` only if new Pi-specific instability appears.
- otherwise keep this as baseline evidence for `OPS-007` runbook freeze.

## Open Blockers
- none for COL-006 scope.

## Verification Status
- `ssh signaldesk-pi "echo SSH_OK && whoami && hostname"` -> `SSH_OK / admin / ubuntu`
- `ssh signaldesk-pi "docker --version && docker compose version"` -> `Docker 28.4.0 / Compose v2.39.2`
- `docker compose ... run --rm collector-bootstrap` on Pi -> `migrate: ok`
- `docker compose ... run --rm collector-runner` on Pi -> `rows_read=2 inserted=2 upserted=0`
- remote `spool-evidence.sql` -> 2 accepted rows present
- `docker compose ... run --rm collector-shipper` on Pi -> `touched_rows=2`, `simulate-offline`
- remote `spool-evidence.sql` after shipper -> `offline_retry`, `storage_unavailable`, `central_offline_192.168.0.200`
- remote `spool-idempotency.sql` -> `total_rows=2`, `distinct_idempotency_keys=2`, `total_ingest_count=2`
- `docker compose ... ps` -> `collector-db` healthy

## Files In Progress
- coordination/tasks.yaml
- coordination/handoffs/COL-006.md
- coordination/resume/COL-006.md
- docs/ops/deploy-runbook.md

## Last Commit And Push
- commit: `3f7cc8d` (`COL-006 ops: record Raspberry Pi deploy smoke evidence`)
- push: `origin/codex/col-006` updated on 2026-03-09

## Notes For Next Session
- exact command/output evidence is recorded in `coordination/handoffs/COL-006.md`.
