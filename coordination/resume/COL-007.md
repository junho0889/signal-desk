# Resume COL-007

## Task
- id: COL-007
- role: signal-desk-collector
- branch: codex/col-007
- worktree: E:\source\signal-desk-worktrees\collector-007
- last updated: 2026-03-08

## Current State
- Blocked at SSH auth gate for Raspberry Pi deployment.

## Last Completed
- Reproduced the exact COL-006 failing command and captured verbose SSH auth diagnostics.
- Isolated root cause: no usable local SSH private key for non-interactive login to `admin@192.168.0.33`.
- Added focused collector deployment-path fixes:
  - `infra/collector/pi-remote-smoke.ps1` (auth preflight + remote smoke sequence)
  - collector Pi auth gate and command surface in `docs/ops/deploy-runbook.md`
- Reran remote boot/ingest/spool query command sequence; all remained blocked by the same SSH auth failure.

## Next Exact Step
- Provision valid SSH auth for `admin@192.168.0.33` (authorized key or equivalent), then rerun:
  - `ssh ... "echo SSH_OK"`
  - `powershell -ExecutionPolicy Bypass -File infra/collector/pi-remote-smoke.ps1`

## Open Blockers
- `ssh -o BatchMode=yes -o ConnectTimeout=10 -p 22 admin@192.168.0.33 "echo SSH_OK"` returns:
  - `Permission denied (publickey,password).`
- `.ssh` directory on this machine currently has `known_hosts` only; no default private key file.

## Verification Status
- reproduced failure:
  - `ssh -o BatchMode=yes -o ConnectTimeout=10 -p 22 admin@192.168.0.33 "echo SSH_OK"` -> exit `255`
- root cause diagnostics:
  - `ssh -vvv ...` -> auth methods `publickey,password`, no local identities found
  - `Test-NetConnection 192.168.0.33 -Port 22` -> reachable
- rerun attempts after fix:
  - remote docker version, compose version, boot, bootstrap, runner, spool query commands -> all exit `255` with `Permission denied (publickey,password).`
- script check:
  - `powershell -ExecutionPolicy Bypass -File infra/collector/pi-remote-smoke.ps1` -> `Missing SSH private key: C:\Users\admin\.ssh\id_ed25519`

## Files In Progress
- coordination/tasks.yaml
- coordination/handoffs/COL-007.md
- coordination/resume/COL-007.md
- docs/ops/deploy-runbook.md
- infra/collector/

## Last Commit And Push
- commit:
- push:

## Notes For Next Session
- Do not proceed to runtime boot steps until SSH preflight returns `SSH_OK`.
