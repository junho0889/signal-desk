## Task
- id: OPS-002
- role: signal-desk-ops
- branch: worker/ops-002
- worktree: E:\source\signal-desk-worktrees\ops-002
- last updated: 2026-03-08

## Current State
- OPS-002 operational baseline is implemented in `infra/local/` and ops docs.
- full local stack (`postgres`, `api`, `jobs`) is up and healthy after compose bring-up.
- awaiting QA review and orchestrator acceptance.

## Last Completed
- wired compose services for `api` and `jobs` with health checks and restart policy.
- added local init mount `infra/local/postgres-init/001-bootstrap.sh` to keep bootstrap deterministic.
- updated deploy runbook and added operations checklist for startup, health, backup, restore, rollback.
- validated runtime with endpoint smoke checks and restart checks.

## Next Exact Step
- QA reviews `worker/ops-002` and publishes `coordination/handoffs/OPS-002-QA.md`.

## Open Blockers
- none.

## Verification Status
- `docker compose ... config` -> pass.
- `docker compose ... down -v` + `up -d --build` -> pass.
- `docker compose ... ps` -> `postgres`, `api`, `jobs` all `healthy`.
- `docker compose ... logs postgres/jobs` -> bootstrap and jobs cycles executed.
- API checks (`/healthz`, `/v1/dashboard`, `/v1/keywords`, `/v1/watchlist`, `/v1/alerts`) -> pass.
- role check query for `signaldesk_migrator|signaldesk_app|signaldesk_readonly` -> pass.
- snapshot freshness query (`MAX(as_of_ts)`) -> pass.
- `docker compose ... restart api jobs` + `ps` -> services return to `healthy`.

## Files In Progress
- none.

## Last Commit And Push
- commit: pending current checkpoint
- push: pending current checkpoint

## Notes For Next Session
- if compose env changes, keep `infra/local/.env.example` and runbook/checklist synchronized.
- keep bootstrap mount path `./postgres-init` read-only.
