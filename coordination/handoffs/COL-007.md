## Task
- id: COL-007
- owner: signal-desk-collector
- status: done
- gate_result: PASS

## Scope Delivered
- Added always-on collector daemon mode in `services/collector/signaldesk_collector/main.py` (`run-daemon` command).
- Added daemon env contract in `services/collector/signaldesk_collector/config.py` and `infra/collector/.env.example`.
- Added `collector-daemon` service in `infra/collector/docker-compose.yml` with `restart: unless-stopped`.
- Kept manual bootstrap/runner/shipper/monitor compose services intact.
- Updated `docs/ops/deploy-runbook.md` with exact 24/7 deploy, evidence, restart, and recovery commands for Pi.

## Remote Command Log (Exact)
1. `ssh signaldesk-pi "echo SSH_OK && whoami && hostname"`
   - output:
     - `SSH_OK`
     - `admin`
     - `ubuntu`
2. `ssh signaldesk-pi "docker --version && docker compose version"`
   - output:
     - `Docker version 28.4.0, build d8eb465`
     - `Docker Compose version v2.39.2`
3. `ssh signaldesk-pi "rm -rf /home/admin/signal-desk-collector && mkdir -p /home/admin/signal-desk-collector/infra /home/admin/signal-desk-collector/services"`
   - output: none
4. `scp -r infra/collector signaldesk-pi:/home/admin/signal-desk-collector/infra/`
   - output: none
5. `scp -r services/collector signaldesk-pi:/home/admin/signal-desk-collector/services/`
   - output: none
6. `ssh signaldesk-pi "cp /home/admin/signal-desk-collector/infra/collector/.env.example /home/admin/signal-desk-collector/infra/collector/.env && sed -i 's/^SIGNALDESK_COLLECTOR_NODE_ID=.*/SIGNALDESK_COLLECTOR_NODE_ID=collector-pi-192-168-0-33/' /home/admin/signal-desk-collector/infra/collector/.env && sed -i 's/^SIGNALDESK_COLLECTOR_DAEMON_INTERVAL_SECONDS=.*/SIGNALDESK_COLLECTOR_DAEMON_INTERVAL_SECONDS=15/' /home/admin/signal-desk-collector/infra/collector/.env && sed -i 's/^SIGNALDESK_COLLECTOR_DAEMON_SHIP_EVERY_CYCLES=.*/SIGNALDESK_COLLECTOR_DAEMON_SHIP_EVERY_CYCLES=1/' /home/admin/signal-desk-collector/infra/collector/.env && sed -i 's/^SIGNALDESK_COLLECTOR_DAEMON_STREAM_MODE=.*/SIGNALDESK_COLLECTOR_DAEMON_STREAM_MODE=true/' /home/admin/signal-desk-collector/infra/collector/.env && sed -i 's/^SIGNALDESK_COLLECTOR_DAEMON_EVENT_STEP_MINUTES=.*/SIGNALDESK_COLLECTOR_DAEMON_EVENT_STEP_MINUTES=1/' /home/admin/signal-desk-collector/infra/collector/.env && sed -i 's/^SIGNALDESK_COLLECTOR_DAEMON_MAX_CYCLES=.*/SIGNALDESK_COLLECTOR_DAEMON_MAX_CYCLES=0/' /home/admin/signal-desk-collector/infra/collector/.env && grep -E '^SIGNALDESK_COLLECTOR_(NODE_ID|DAEMON_INTERVAL_SECONDS|DAEMON_SHIP_EVERY_CYCLES|DAEMON_STREAM_MODE|DAEMON_EVENT_STEP_MINUTES|DAEMON_MAX_CYCLES)' /home/admin/signal-desk-collector/infra/collector/.env"`
   - output:
     - `SIGNALDESK_COLLECTOR_NODE_ID=collector-pi-192-168-0-33`
     - `SIGNALDESK_COLLECTOR_DAEMON_INTERVAL_SECONDS=15`
     - `SIGNALDESK_COLLECTOR_DAEMON_SHIP_EVERY_CYCLES=1`
     - `SIGNALDESK_COLLECTOR_DAEMON_STREAM_MODE=true`
     - `SIGNALDESK_COLLECTOR_DAEMON_EVENT_STEP_MINUTES=1`
     - `SIGNALDESK_COLLECTOR_DAEMON_MAX_CYCLES=0`
7. `ssh signaldesk-pi "cd /home/admin/signal-desk-collector && docker compose -f infra/collector/docker-compose.yml --env-file infra/collector/.env down -v --remove-orphans"`
   - output includes:
     - `Container signaldesk_collector-collector-daemon-1  Removed`
     - `Container signaldesk_collector-collector-db-1  Removed`
     - `Volume signaldesk_collector_collector-postgres-data  Removed`
8. `ssh signaldesk-pi "cd /home/admin/signal-desk-collector && docker compose -f infra/collector/docker-compose.yml --env-file infra/collector/.env up -d collector-db"`
   - output includes:
     - `Container signaldesk_collector-collector-db-1  Started`
9. `ssh signaldesk-pi "cd /home/admin/signal-desk-collector && docker compose -f infra/collector/docker-compose.yml --env-file infra/collector/.env run --rm collector-bootstrap"`
   - output:
     - `migrate: ok`
10. `ssh signaldesk-pi "cd /home/admin/signal-desk-collector && docker compose -f infra/collector/docker-compose.yml --env-file infra/collector/.env up -d collector-daemon"`
    - output includes:
      - `Container signaldesk_collector-collector-db-1  Healthy`
      - `Container signaldesk_collector-collector-daemon-1  Started`
11. `ssh signaldesk-pi "cd /home/admin/signal-desk-collector && docker compose -f infra/collector/docker-compose.yml --env-file infra/collector/.env ps"`
    - output:
      - `signaldesk_collector-collector-daemon-1 ... Up`
      - `signaldesk_collector-collector-db-1 ... Up (healthy)`
12. `ssh signaldesk-pi "docker inspect -f '{{.HostConfig.RestartPolicy.Name}}' signaldesk_collector-collector-daemon-1"`
    - output:
      - `unless-stopped`
13. Snapshot loop command:
    - command:
      - `for ($i=1; $i -le 3; $i++) { Start-Sleep -Seconds 20; Write-Output ("=== SNAPSHOT " + $i + " " + (Get-Date -Format o) + " ==="); ssh signaldesk-pi "cd /home/admin/signal-desk-collector && docker compose -f infra/collector/docker-compose.yml --env-file infra/collector/.env logs collector-daemon --tail=6"; ssh signaldesk-pi "cd /home/admin/signal-desk-collector && cat infra/collector/queries/spool-evidence.sql | docker compose -f infra/collector/docker-compose.yml --env-file infra/collector/.env exec -T collector-db psql -U collector -d signaldesk_collector -f -"; ssh signaldesk-pi "cd /home/admin/signal-desk-collector && cat infra/collector/queries/spool-idempotency.sql | docker compose -f infra/collector/docker-compose.yml --env-file infra/collector/.env exec -T collector-db psql -U collector -d signaldesk_collector -f -" }`
    - output (exact idempotency totals per snapshot):
      - snapshot 1: `total_rows=6`, `distinct_idempotency_keys=6`, `total_ingest_count=6`
      - snapshot 2: `total_rows=10`, `distinct_idempotency_keys=10`, `total_ingest_count=10`
      - snapshot 3: `total_rows=12`, `distinct_idempotency_keys=12`, `total_ingest_count=12`
    - output (spool evidence state per snapshot):
      - `status=pending`, `ingest_status=accepted`, `quality_state=accepted`, `transport_status=offline_retry`, `reason_code=storage_unavailable`, `last_intake_status=retryable_failure`
14. Metadata completeness and quality distribution:
    - command:
      - `ssh signaldesk-pi "cd /home/admin/signal-desk-collector && docker compose -f infra/collector/docker-compose.yml --env-file infra/collector/.env exec -T collector-db psql -U collector -d signaldesk_collector -c 'SELECT COUNT(*) AS total_rows, COUNT(*) FILTER (WHERE COALESCE(octet_length(collector_node_id),0)=0 OR COALESCE(octet_length(payload_hash),0)=0 OR COALESCE(octet_length(idempotency_key),0)=0 OR COALESCE(octet_length(publisher_domain),0)=0 OR COALESCE(octet_length(canonical_url),0)=0) AS rows_with_missing_required_metadata FROM spool_items;' -c 'SELECT quality_state, COUNT(*) AS row_count FROM spool_items GROUP BY quality_state ORDER BY quality_state;'"`
    - output:
      - `total_rows=18`
      - `rows_with_missing_required_metadata=0`
      - `quality_state=accepted row_count=18`
15. Retry evidence (shipper activity on daemon cycles):
    - command:
      - `ssh signaldesk-pi "cd /home/admin/signal-desk-collector && docker compose -f infra/collector/docker-compose.yml --env-file infra/collector/.env exec -T collector-db psql -U collector -d signaldesk_collector -c 'SELECT COUNT(*) AS total_rows, MAX(retry_count) AS max_retry_count, MIN(retry_count) AS min_retry_count, COUNT(*) FILTER (WHERE retry_count >= 1) AS rows_with_retry FROM spool_items;'" `
    - output:
      - `total_rows=28`
      - `max_retry_count=14`
      - `min_retry_count=0`
      - `rows_with_retry=20`

## Local Verification
- `python -m compileall services/collector/signaldesk_collector`
  - output: listed package + fixtures, no compile errors
- `docker compose -f infra/collector/docker-compose.yml --env-file infra/collector/.env.example config`
  - output: `compose config ok`

## Evidence Tables
### Idempotency Growth
| snapshot | total_rows | distinct_idempotency_keys | total_ingest_count |
| --- | ---: | ---: | ---: |
| 1 | 6 | 6 | 6 |
| 2 | 10 | 10 | 10 |
| 3 | 12 | 12 | 12 |

### Metadata and Quality
| query | result |
| --- | --- |
| missing required metadata rows | 0 |
| quality_state distribution | accepted=18 |
| collector retry evidence | max_retry_count=14, rows_with_retry=20 |

## Gate Criteria Check
- collector-db healthy: PASS
- collector-daemon stays up continuously: PASS
- repeat ingest cycles without crash (>=3): PASS
- spool evidence + idempotency queries after each cycle: PASS
- metadata completeness + quality distribution confirmed: PASS
- blocker list explicit: no blocker

## Files Touched
- `coordination/tasks.yaml`
- `coordination/handoffs/COL-007.md`
- `coordination/resume/COL-007.md`
- `docs/ops/deploy-runbook.md`
- `infra/collector/.env.example`
- `infra/collector/docker-compose.yml`
- `services/collector/`
