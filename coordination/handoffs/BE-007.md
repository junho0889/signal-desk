## Task
- id: BE-007
- owner: signal-desk-backend
- status: done

## What Changed
- updated `docs/ops/service-model.md` to freeze central job order for explainable model publishing:
  - normalization -> trust -> feature snapshot -> ranking -> publish -> alert evaluation
- documented stage-level failure behavior and downstream stop/resume rules
- defined publish idempotency key assumptions and reprocessing semantics that avoid raw reingest in normal publish-recovery cases
- updated `docs/backend/api-contract.md` with additive `v1` publish assumptions:
  - internal publish boundary
  - optional `publish_meta` guidance
  - compatibility guardrails for existing clients
  - publish retry idempotency and fallback serving assumptions

## Stable Assumptions For Implementation
- API must serve published snapshots only; internal in-flight artifacts are not API-serving records
- alert evaluation runs only from published artifacts
- publish retries with the same key are idempotent
- failed current-window publish does not break clients; API serves last successful snapshot with degraded freshness signaling

## Verification
- commands:
  - `git -C E:\source\signal-desk-worktrees\be-007 diff --check`
- result:
  - passed (no diff-check errors)

## Open Blockers / Missing Upstream Handoffs
- `coordination/handoffs/BE-006.md` not present in this worktree
- `coordination/handoffs/MODEL-002.md` not present in this worktree
- `coordination/handoffs/TRUST-002.md` not present in this worktree
- `coordination/handoffs/BE-005.md` not present on `main` in this worktree lineage

## Next Step
- downstream implementation lanes can use the frozen order and additive assumptions in this handoff
- if MODEL/TRUST/STORAGE lanes require additional publish metadata, add fields under the optional `publish_meta` path only
