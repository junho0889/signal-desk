## Task
- id: OPS-004
- owner: signal-desk-ops
- status: done

## What Changed
- added `scripts/orchestrator/mobile-preview.ps1` to standardize `doctor`, `verify`, and `run` flows for SignalDesk mobile preview sessions
- rewrote `docs/ops/app-preview.md` around repeatable mock and live verification, current app runtime defines, and repo-vs-machine responsibility boundaries
- updated `docs/ops/quality-gates.md` so Flutter preview checks now require explicit mode and blocker reporting
- added the missing task dispatch file to this worktree so the branch carries its own OPS-004 scope

## Current State
- preview workflow is documented for both `mock` and `live` modes
- helper commands now recommend `powershell -ExecutionPolicy Bypass -File ...` so they do not require global execution policy changes
- the current environment remains blocked for runtime verification because Flutter is not installed here
- the current `app/mobile` tree also lacks generated platform folders, so `run` will remain blocked until the mobile owner adds the required platform scaffold in the active mobile worktree

## Verification
- commands:
  - `powershell -File .\scripts\orchestrator\mobile-preview.ps1 -Command doctor -Mode mock -AppPath E:\source\signal-desk-worktrees\ops-004\app\mobile`
  - `powershell -ExecutionPolicy Bypass -File .\scripts\orchestrator\mobile-preview.ps1 -Command doctor -Mode mock -AppPath E:\source\signal-desk-worktrees\ops-004\app\mobile`
  - `if (Get-Command flutter -ErrorAction SilentlyContinue) { Get-Command flutter -ErrorAction SilentlyContinue | Select-Object Source, Name } else { Write-Output 'flutter-missing' }`
  - `Get-ChildItem .\app\mobile -Directory | Select-Object Name`
  - `Select-String -Path .\docs\ops\app-preview.md,.\docs\ops\quality-gates.md,.\docs\mobile\implementation-notes.md -Pattern 'SIGNALDESK_USE_MOCK|SIGNALDESK_API_BASE_URL|mobile-preview.ps1|Flutter SDK|platform scaffold' | Select-Object Path, LineNumber, Line`
  - `git diff --check`
- result:
  - direct `powershell -File` execution was blocked by local script execution policy, so the docs now standardize the per-process `-ExecutionPolicy Bypass` wrapper instead of changing machine-wide policy
  - helper `doctor` now fails with an explicit Flutter blocker: `Flutter SDK was not found. Install Flutter manually or set SIGNALDESK_FLUTTER_BIN to flutter.bat.`
  - `Get-Command flutter` returned `flutter-missing`
  - `app/mobile` currently contains only `lib/`, confirming target platform folders are absent in this repo snapshot
  - docs consistency review confirmed the preview docs align with the mobile runtime defines in `docs/mobile/implementation-notes.md`
  - `git diff --check` passed apart from line-ending warnings on the edited Markdown files

## Blockers
- Flutter SDK is not installed in this worker environment
- target platform scaffolding (`android/`, `web/`, `windows/`) is not present under the current `app/mobile` tree, so preview smoke cannot run until the mobile owner adds the needed platform folder in the active mobile worktree

## Next Step
- have a Flutter-capable session rerun `mobile-preview.ps1` in `verify` and `run` modes against the active mobile worktree after the needed platform scaffold exists
- route the result through `QA-003` for integration review

## Files Touched
- `coordination/dispatches/OPS-004.md`
- `coordination/handoffs/OPS-004.md`
- `docs/ops/app-preview.md`
- `docs/ops/quality-gates.md`
- `scripts/orchestrator/mobile-preview.ps1`
