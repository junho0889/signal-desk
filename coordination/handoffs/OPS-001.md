## Task
- id: OPS-001
- owner: signal-desk-ops
- status: in_progress (deliverables updated; awaiting QA/orchestrator acceptance)

## What Changed
- rewrote `docs/ops/deploy-runbook.md` with deterministic startup, checks, backup/restore, release, rollback, and escalation flows
- rewrote `docs/ops/service-model.md` with phase-based runtime topology, service responsibilities, reliability targets, monitoring, and security model
- aligned operational rules with current compose reality (`postgres` only) and target MVP runtime (`postgres` + `api` + `jobs`)

## Changed Files
- docs/ops/deploy-runbook.md
- docs/ops/service-model.md
- coordination/handoffs/OPS-001.md
- coordination/resume/OPS-001.md

## Verification
- commands:
  - `Select-String -Path .\infra\local\docker-compose.yml -Pattern 'postgres|signaldesk-internal|127.0.0.1|docker-entrypoint-initdb.d'`
  - `Select-String -Path .\docs\ops\deploy-runbook.md -Pattern 'docker compose -f infra/local/docker-compose.yml|backup|restore|Rollback Checklist|Security Checks'`
  - `Select-String -Path .\docs\ops\service-model.md -Pattern 'Phase 0|Phase 1|postgres|api|jobs|data freshness|Release Readiness Gate'`
  - `Select-String -Path .\docs\backend\postgres-security.md -Pattern 'signaldesk_migrator|signaldesk_app|signaldesk_readonly|never expose PostgreSQL'`
- result:
  - runbook and service model match current compose topology and security baseline.
  - planned API/jobs onboarding is documented without contradicting current infra files.
- manual consistency review assertions:
  - least-privilege role model is preserved across operations text.
  - local-first deployment path remains reversible with explicit rollback/restore actions.

## Blockers
- none.

## Operational Gaps To Implement Later
- `api` and `jobs` compose services are still not defined in `infra/local/docker-compose.yml`.
- health endpoints and job heartbeat checks require service code implementation.

## Next Step
- QA review of OPS-001 docs and verdict handoff (`coordination/handoffs/OPS-001-QA.md`).
