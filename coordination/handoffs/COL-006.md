## Task
- id: COL-006
- owner: signal-desk-collector
- status: done

## What Changed
- confirmed SSH key login to Raspberry Pi `192.168.0.33` as `admin`
- verified remote Docker and Docker Compose availability
- synced collector assets to Pi:
  - `/home/admin/signal-desk-collector/infra/collector`
  - `/home/admin/signal-desk-collector/services/collector`
- cleaned stale conflicting container name (`signaldesk-collector-db`)
- booted collector stack on Pi and ran fixture ingest plus spool evidence SQL checks

## Remote Command Log (Exact)
1. `ssh signaldesk-pi "echo SSH_OK && whoami && hostname"`
   - result: `SSH_OK`, `admin`, `ubuntu`
2. `ssh signaldesk-pi "docker --version && docker compose version"`
   - result: `Docker version 28.4.0`, `Docker Compose version v2.39.2`
3. `ssh signaldesk-pi "rm -rf ~/signal-desk-collector && mkdir -p ~/signal-desk-collector/infra ~/signal-desk-collector/services"`
   - result: success
4. `scp -r "E:\source\signal-desk-worktrees\collector-006\infra\collector" signaldesk-pi:/home/admin/signal-desk-collector/infra/`
   - result: success
5. `scp -r "E:\source\signal-desk-worktrees\collector-006\services\collector" signaldesk-pi:~/signal-desk-collector/services/`
   - result: success
6. `ssh signaldesk-pi "cd /home/admin/signal-desk-collector && cp infra/collector/.env.example infra/collector/.env"`
   - result: success
7. `ssh signaldesk-pi "docker rm -f signaldesk-collector-db || true; docker rm -f signaldesk-collector-bootstrap signaldesk-collector-runner signaldesk-collector-shipper signaldesk-collector-monitor || true"`
   - result: removed stale `signaldesk-collector-db`; others not found
8. `ssh signaldesk-pi "cd /home/admin/signal-desk-collector && docker compose -f infra/collector/docker-compose.yml --env-file infra/collector/.env down -v --remove-orphans"`
   - result: old collector network/volume removed
9. `ssh signaldesk-pi "cd /home/admin/signal-desk-collector && docker compose -f infra/collector/docker-compose.yml --env-file infra/collector/.env up -d collector-db"`
   - result: collector-db started
10. `ssh signaldesk-pi "cd /home/admin/signal-desk-collector && docker compose -f infra/collector/docker-compose.yml --env-file infra/collector/.env run --rm collector-bootstrap"`
    - result: `migrate: ok`
11. `ssh signaldesk-pi "cd /home/admin/signal-desk-collector && docker compose -f infra/collector/docker-compose.yml --env-file infra/collector/.env run --rm collector-runner"`
    - result: `rows_read=2`, `inserted=2`, `upserted=0`
12. `ssh signaldesk-pi "cd /home/admin/signal-desk-collector && cat infra/collector/queries/spool-evidence.sql | docker compose -f infra/collector/docker-compose.yml --env-file infra/collector/.env exec -T collector-db psql -U collector -d signaldesk_collector -f -"`
    - result: 2 rows in `spool_items`, both `status=pending`, `ingest_status=accepted`, `quality_state=accepted`, `transport_status=spooled`
13. `ssh signaldesk-pi "cd /home/admin/signal-desk-collector && docker compose -f infra/collector/docker-compose.yml --env-file infra/collector/.env run --rm collector-shipper"`
    - result: `mode=simulate-offline`, `simulated_intake_status=retryable_failure`, `touched_rows=2`
14. `ssh signaldesk-pi "cd /home/admin/signal-desk-collector && cat infra/collector/queries/spool-evidence.sql | docker compose -f infra/collector/docker-compose.yml --env-file infra/collector/.env exec -T collector-db psql -U collector -d signaldesk_collector -f -"`
    - result: 2 rows persisted with `transport_status=offline_retry`, `reason_code=storage_unavailable`, `last_error_code=central_offline_192.168.0.200`
15. `ssh signaldesk-pi "cd /home/admin/signal-desk-collector && cat infra/collector/queries/spool-idempotency.sql | docker compose -f infra/collector/docker-compose.yml --env-file infra/collector/.env exec -T collector-db psql -U collector -d signaldesk_collector -f -"`
    - result: `total_rows=2`, `distinct_idempotency_keys=2`, `total_ingest_count=2`
16. `ssh signaldesk-pi "cd /home/admin/signal-desk-collector && docker compose -f infra/collector/docker-compose.yml --env-file infra/collector/.env ps"`
    - result: `signaldesk_collector-collector-db-1` is `Up (healthy)`

## Blocker Classification
- auth blocker: none
- docker blocker: none
- collector-runtime blocker: none

## Deployment Readiness Verdict
- Pi `192.168.0.33` is deployment-ready for current collector v1 smoke path.
- central intake remains simulated (`simulate-offline`), so end-to-end central delivery is still a downstream backend/ops contract lane.

## Files Touched
- `coordination/tasks.yaml`
- `coordination/handoffs/COL-006.md`
- `coordination/resume/COL-006.md`
- `docs/ops/deploy-runbook.md`
- `infra/collector/`
- `services/collector/`
