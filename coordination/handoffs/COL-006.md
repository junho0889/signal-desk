## Task
- id: COL-006
- owner: signal-desk-collector
- status: blocked

## What Changed
- claimed `COL-006` in `coordination/tasks.yaml` (`pending` -> `in_progress`)
- imported accepted collector stack assets from `origin/codex/col-005` into this worktree so Pi deployment files exist locally:
  - `infra/collector/`
  - `services/collector/`

## Remote Command Log (Exact)
- precheck:
  - command: `ssh -V`
  - outcome:
    - `OpenSSH_for_Windows_9.5p2, LibreSSL 3.8.2`
- remote login gate:
  - command: `ssh -o BatchMode=yes -o ConnectTimeout=10 -p 22 admin@192.168.0.33 "echo SSH_OK"`
  - outcome:
    - `admin@192.168.0.33: Permission denied (publickey,password).`

## Stop Condition Triggered
- dispatch rule requires stopping if login fails.
- because SSH auth failed, these steps were not run:
  - remote `docker --version`
  - remote `docker compose version`
  - asset sync/copy to Pi
  - remote compose bootstrap/start
  - remote fixture ingest
  - remote spool evidence query

## Blocker Classification
- auth blocker: SSH credential/access failure for `admin@192.168.0.33:22`
- docker blocker: not evaluated due auth gate failure
- runtime blocker: not evaluated due auth gate failure

## Next Step
- provide working SSH credentials or authorized key access for `admin@192.168.0.33`.
- rerun COL-006 from SSH gate and continue only if login succeeds.

## Files Touched
- `coordination/tasks.yaml`
- `coordination/handoffs/COL-006.md`
- `coordination/resume/COL-006.md`
- `infra/collector/`
- `services/collector/`
