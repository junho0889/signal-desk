## Task
- id: OPS-007
- owner: signal-desk-ops
- status: blocked

## What Changed
- claimed `OPS-007` in `coordination/tasks.yaml`
- added the missing `OPS-007` dispatch, handoff, and resume files to this worktree
- collected the first real remote evidence from Raspberry Pi `192.168.0.33`

## Current State
- the host at `192.168.0.33` is reachable over SSH
- authenticated collector evidence is still missing, so no Pi deploy, restart, log, inspect, or rollback runbook steps were frozen
- `docs/ops/deploy-runbook.md` and `docs/ops/quality-gates.md` remain unchanged to avoid speculative operator guidance

## Verification
- commands:
  - `ssh -o BatchMode=yes -o StrictHostKeyChecking=no -o ConnectTimeout=5 ubuntu@192.168.0.33 "hostname"`
  - `ssh -o BatchMode=yes -o StrictHostKeyChecking=no -o ConnectTimeout=5 pi@192.168.0.33 "hostname"`
  - `ssh -o BatchMode=yes -o StrictHostKeyChecking=no -o ConnectTimeout=5 admin@192.168.0.33 "hostname"`
  - `git diff --check`
- result:
  - `192.168.0.33` responded and presented an SSH host key
  - `ubuntu`, `pi`, and `admin` all failed with `Permission denied (publickey,password)`
  - no authenticated remote commands were available to support a real runbook freeze
  - `git diff --check` must be rerun after the coordination files are committed

## Open Blockers
- `COL-006` handoff evidence is still missing
- no working SSH principal or key is available yet for `192.168.0.33`
- without authenticated remote access, deployment, restart, logs, inspect, and rollback commands would be guesswork

## Next Step
- obtain the real `COL-006` or `COL-007` handoff, or a working SSH principal for `192.168.0.33`
- once authenticated access exists, freeze only the remote commands that are actually exercised
