## Task
- id: BE-003
- owner: signal-desk-backend
- status: done

## What Changed
- added an internal notification payload contract for watchlist alert delivery
- added `delivery.py` with notification payload building and `none|stdout` sink dispatch baseline
- extended alert evaluation output so persisted alerts can be transformed into notification-ready payloads without changing the public mobile API surface
- documented configuration and delivery behavior in backend notes and the contract doc

## Current State
- `evaluate-alerts` still persists alert rows as before
- notification-ready payloads can now be generated for downstream push delivery
- local verification uses the `stdout` sink without requiring an external provider

## Verification
- commands:
  - `python -m compileall services\jobs\signaldesk_jobs`
  - `$env:SIGNALDESK_NOTIFICATION_SINK='stdout'; python -m services.jobs.signaldesk_jobs.main evaluate-alerts`
  - inline Python check for `build_notification_deliveries(...)`
- result:
  - jobs package compiled successfully
  - alert evaluation still completed without runtime errors
  - delivery payload builder produced the expected push-oriented payload shape

## Blockers
- no external push provider integration yet by design

## Next Step
- route this through `QA-003`
- continue `APP-004` and `OPS-004` in parallel

## Files Touched
- `services/jobs/signaldesk_jobs/alerts.py`
- `services/jobs/signaldesk_jobs/delivery.py`
- `services/jobs/signaldesk_jobs/config.py`
- `services/jobs/signaldesk_jobs/main.py`
- `docs/backend/api-contract.md`
- `docs/backend/implementation-notes.md`
- `coordination/decision-log.md`
