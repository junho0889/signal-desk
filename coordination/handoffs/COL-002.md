## Task
- id: COL-002
- owner: signal-desk-collector
- status: done

## What Changed
- defined collector v1 as a PC-first Docker Compose runtime with explicit services: `collector-db`, `collector-runner`, `collector-shipper`, and optional `collector-monitor`
- locked v1 spool durability on local PostgreSQL and explicitly excluded RabbitMQ
- documented spool state model, acknowledgement mapping, and retry behavior aligned with backend intake response classes
- documented 30-day retention rules for unacknowledged/dead-letter records plus guarded pruning rules
- documented that central host `192.168.0.200` can be offline for long windows while collection continues locally
- documented promotion path from PC Compose runtime to Ubuntu on Raspberry Pi with stable service boundaries

## Current State
- `docs/ops/collector-runtime-v1.md` now contains the frozen collector v1 runtime shape and spool contract baseline
- `docs/ops/pi-collector-node.md` now reflects PC-first execution plus later Ubuntu-on-Pi packaging model
- `coordination/tasks.yaml` marks `COL-002` as `done`

## Verification
- commands:
  - `git diff --check`
  - `Get-ChildItem -Path docs/architecture,docs/ops,docs/backend -Recurse -File | Select-String -Pattern 'RabbitMQ|collector-db|collector-runner|collector-shipper|192.168.0.200|30 days|accepted|duplicate|rejected|retryable_failure|PostgreSQL spool'`
- result:
  - `git diff --check` passed (no whitespace/conflict issues)
  - terminology and response-class alignment confirmed across architecture, ops, and backend intake docs

## Blockers
- `coordination/handoffs/COL-001.md` is not present in this worktree branch snapshot; work proceeded using current architecture + collector docs and backend intake contract

## Next Step
- backend lane (`BE-005`) can finalize intake endpoint/path/auth details using this v1 runtime and acknowledgement mapping baseline
- ops lane (`OPS-005`) can define the compose files/runbook for PC-first operation and Pi promotion without adding broker infrastructure

## Files Touched
- `docs/ops/collector-runtime-v1.md`
- `docs/ops/pi-collector-node.md`
- `coordination/tasks.yaml`
- `coordination/handoffs/COL-002.md`
- `coordination/resume/COL-002.md`
