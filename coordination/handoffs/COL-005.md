## Task
- id: COL-005
- owner: signal-desk-collector
- status: done

## What Changed
- imported the runnable collector stack into this worktree under:
  - `infra/collector/`
  - `services/collector/`
- aligned implementation with frozen COL-004 / BE-008 / BE-009 / OPS-006 contracts by fixing:
  - spool schema field names and metadata coverage
  - intake-status and reason-code tracking
  - migration safety for pre-existing old spool schema rows
  - compose env surface for deterministic local and Pi behavior
  - evidence SQL query shape to include frozen metadata and status fields
- aligned owned docs required by implementation and verification:
  - `docs/data/collector-metadata-quality-v1.md`
  - `docs/ops/collector-runtime-v1.md`

## Contract Review Matrix
- `COL-003` (collector implementation baseline):
  - before: stack absent in `collector-005` worktree
  - fix: imported full runnable stack and smoke assets
  - result: matched
- `COL-004` (frozen metadata and quality states):
  - before: missing `ingest_status`, `adapter_version`, `retrieval_status`, `source_cursor` in implementation path
  - fix: added these fields and populated them in ingest flow
  - result: matched
- `BE-008` (storage metadata and quality persistence expectations):
  - before: naming mismatch (`payload_hash_sha256`, `*_utc` fields) and missing intake/quality linkage fields for downstream persistence mapping
  - fix: moved to canonical names (`payload_hash`, `collected_at`, `upstream_event_at`, etc.) and added compatibility migration SQL from legacy columns
  - result: matched for local spool contract surface
- `BE-009` (intake validation outcomes and reason codes):
  - before: no explicit `last_intake_status`, limited shipper outcome semantics
  - fix: added `last_intake_status`, `reason_code`, `transport_status`; added shipper simulation mapping for `retryable_failure` plus support for intake-mode outcomes
  - result: matched for local simulation surface
- `OPS-006` (exact smoke and evidence workflow):
  - before: runtime doc used shell redirection syntax incompatible with PowerShell; compose runtime lacked some explicit env controls
  - fix: corrected query command form and expanded env keys with deterministic defaults
  - result: matched

## Verification
- commands:
  - `docker compose -f infra/collector/docker-compose.yml --env-file infra/collector/.env.example down -v --remove-orphans`
  - `docker compose -f infra/collector/docker-compose.yml --env-file infra/collector/.env.example build --no-cache collector-bootstrap collector-runner collector-shipper collector-monitor`
  - `docker compose -f infra/collector/docker-compose.yml --env-file infra/collector/.env.example up -d collector-db`
  - `docker compose -f infra/collector/docker-compose.yml --env-file infra/collector/.env.example run --rm collector-bootstrap`
  - `docker compose -f infra/collector/docker-compose.yml --env-file infra/collector/.env.example run --rm collector-runner`
  - `Get-Content -Raw 'infra/collector/queries/spool-evidence.sql' | docker compose -f infra/collector/docker-compose.yml --env-file infra/collector/.env.example exec -T collector-db psql -U collector -d signaldesk_collector -f -`
  - `docker compose -f infra/collector/docker-compose.yml --env-file infra/collector/.env.example run --rm collector-shipper`
  - `Get-Content -Raw 'infra/collector/queries/spool-evidence.sql' | docker compose -f infra/collector/docker-compose.yml --env-file infra/collector/.env.example exec -T collector-db psql -U collector -d signaldesk_collector -f -`
  - `docker compose -f infra/collector/docker-compose.yml --env-file infra/collector/.env.example restart collector-db`
  - `docker compose -f infra/collector/docker-compose.yml --env-file infra/collector/.env.example run --rm collector-runner`
  - `Get-Content -Raw 'infra/collector/queries/spool-idempotency.sql' | docker compose -f infra/collector/docker-compose.yml --env-file infra/collector/.env.example exec -T collector-db psql -U collector -d signaldesk_collector -f -`
  - `python -m compileall services/collector/signaldesk_collector`
  - `git diff --check`
  - `docker compose -f infra/collector/docker-compose.yml --env-file infra/collector/.env.example down`
- result:
  - rerun date: `2026-03-08`
  - bootstrap succeeded: `migrate: ok`
  - ingest #1: `rows_read=2`, `inserted=2`, `upserted=0`
  - evidence after ingest:
    - `spool_id=1f49e40b-27fc-59b1-9e77-bc109143b09b`
    - `spool_id=f51ac616-e3d2-5ba9-bc34-f6c454bd14b4`
    - both rows `status=pending`, `ingest_status=accepted`, `quality_state=accepted`, `transport_status=spooled`
  - shipper simulation: `mode=simulate-offline`, `simulated_intake_status=retryable_failure`, `touched_rows=2`
  - evidence after ship:
    - both rows `status=pending`
    - both rows `ingest_status=accepted`
    - both rows `quality_state=accepted`
    - both rows `transport_status=offline_retry`
    - both rows `last_intake_status=retryable_failure`
    - both rows `reason_code=storage_unavailable`
    - both rows `last_error_code=central_offline_192.168.0.200`
  - ingest #2 rerun: `rows_read=2`, `inserted=0`, `upserted=2`
  - idempotency query:
    - `total_rows=2`
    - `distinct_idempotency_keys=2`
    - `total_ingest_count=4`
  - `python -m compileall ...` passed
  - `git diff --check` passed (line-ending warnings only)

## Readiness / Blockers
- readiness for `COL-006`:
  - local collector stack is runnable and repeatable
  - metadata evidence queries now expose frozen contract fields needed for review
  - migration path handles legacy spool column names and fresh deployments
- remaining blockers (non-local):
  - real central intake endpoint/auth rollout is still pending in upstream backend lanes; shipper currently validates retryable-offline simulation only
  - this is not a blocker for Pi-side local ingest/spool proof, but it is a blocker for end-to-end central delivery readiness

## Files Touched
- `infra/collector/.env.example`
- `infra/collector/README.md`
- `infra/collector/docker-compose.yml`
- `infra/collector/queries/spool-evidence.sql`
- `infra/collector/queries/spool-idempotency.sql`
- `infra/collector/reset-test-db.ps1`
- `services/collector/Dockerfile`
- `services/collector/requirements.txt`
- `services/collector/signaldesk_collector/__init__.py`
- `services/collector/signaldesk_collector/config.py`
- `services/collector/signaldesk_collector/db.py`
- `services/collector/signaldesk_collector/main.py`
- `services/collector/signaldesk_collector/fixtures/news_primary.json`
- `docs/ops/collector-runtime-v1.md`
- `docs/data/collector-metadata-quality-v1.md`
- `coordination/tasks.yaml`
- `coordination/handoffs/COL-005.md`
- `coordination/resume/COL-005.md`
