## Task
- id: COL-005
- owner: signal-desk-collector
- status: done

## What Changed
- applied high-impact, low-risk collector quality fixes in `services/collector/signaldesk_collector/main.py`:
  - metadata normalization for URL, timestamp literals, language, market scope, and whitespace
  - source-aware validation requirements (`news`, `trends`, `market`, `dart`)
  - synthetic canonical URL generation for sources without natural URL fields
  - dedup key hardening with normalized URL/timestamp/title/source inputs
  - shipper retry/state fix so retry counts increment only on retryable failures
  - intake transport-state mapping update (`accepted/duplicate -> delivered`)
- added fixture/unit tests:
  - `services/collector/tests/test_quality_rules.py`
- adjusted SQL verification queries:
  - expanded `spool-evidence.sql` metadata columns
  - expanded `spool-idempotency.sql` with retry-quality checks
  - added `spool-quality-summary.sql`
- updated collector README with exact query commands

## Verification
- commands:
  - pre-fix local baseline:
    - `docker compose -f infra/collector/docker-compose.yml --env-file infra/collector/.env.example down -v --remove-orphans`
    - `docker compose -f infra/collector/docker-compose.yml --env-file infra/collector/.env.example up -d collector-db`
    - `docker compose -f infra/collector/docker-compose.yml --env-file infra/collector/.env.example run --rm collector-bootstrap`
    - `docker compose -f infra/collector/docker-compose.yml --env-file infra/collector/.env.example run --rm collector-runner`
    - `docker compose -f infra/collector/docker-compose.yml --env-file infra/collector/.env.example run --rm -e SIGNALDESK_COLLECTOR_SHIPPER_MODE=simulate-intake -e SIGNALDESK_COLLECTOR_SIMULATED_INTAKE_STATUS=accepted -e SIGNALDESK_COLLECTOR_SIMULATED_REASON_CODE=accepted collector-shipper`
    - `docker compose -f infra/collector/docker-compose.yml --env-file infra/collector/.env.example exec -T collector-db psql -U collector -d signaldesk_collector -c "SELECT spool_id::text, status, last_intake_status, retry_count, transport_status FROM spool_items ORDER BY collected_at;"`
  - post-fix local:
    - `python -m unittest discover -s services/collector/tests -v` (from `services/collector`)
    - `python -m compileall services/collector/signaldesk_collector`
    - `docker compose -f infra/collector/docker-compose.yml --env-file infra/collector/.env.example down -v --remove-orphans`
    - `docker compose -f infra/collector/docker-compose.yml --env-file infra/collector/.env.example build collector-bootstrap collector-runner collector-shipper`
    - `docker compose -f infra/collector/docker-compose.yml --env-file infra/collector/.env.example up -d collector-db`
    - `docker compose -f infra/collector/docker-compose.yml --env-file infra/collector/.env.example run --rm collector-bootstrap`
    - `docker compose -f infra/collector/docker-compose.yml --env-file infra/collector/.env.example run --rm collector-runner`
    - `docker compose -f infra/collector/docker-compose.yml --env-file infra/collector/.env.example run --rm -e SIGNALDESK_COLLECTOR_SHIPPER_MODE=simulate-intake -e SIGNALDESK_COLLECTOR_SIMULATED_INTAKE_STATUS=accepted -e SIGNALDESK_COLLECTOR_SIMULATED_REASON_CODE=accepted collector-shipper`
    - `docker compose -f infra/collector/docker-compose.yml --env-file infra/collector/.env.example exec -T collector-db psql -U collector -d signaldesk_collector -c "SELECT spool_id::text, status, last_intake_status, retry_count, transport_status FROM spool_items ORDER BY collected_at;"`
    - `docker compose -f infra/collector/docker-compose.yml --env-file infra/collector/.env.example run --rm collector-runner`
    - `docker compose -f infra/collector/docker-compose.yml --env-file infra/collector/.env.example run --rm collector-shipper`
    - `Get-Content -Raw infra/collector/queries/spool-evidence.sql | docker compose -f infra/collector/docker-compose.yml --env-file infra/collector/.env.example exec -T collector-db psql -U collector -d signaldesk_collector -f -`
    - `Get-Content -Raw infra/collector/queries/spool-idempotency.sql | docker compose -f infra/collector/docker-compose.yml --env-file infra/collector/.env.example exec -T collector-db psql -U collector -d signaldesk_collector -f -`
    - `Get-Content -Raw infra/collector/queries/spool-quality-summary.sql | docker compose -f infra/collector/docker-compose.yml --env-file infra/collector/.env.example exec -T collector-db psql -U collector -d signaldesk_collector -f -`
    - `docker compose -f infra/collector/docker-compose.yml --env-file infra/collector/.env.example down`
  - post-fix Pi smoke:
    - `ssh -o BatchMode=yes -o ConnectTimeout=10 signaldesk-pi "echo SSH_OK"`
    - `ssh signaldesk-pi "docker --version"`
    - `ssh signaldesk-pi "docker compose version"`
    - `ssh signaldesk-pi "mkdir -p ~/signal-desk/infra ~/signal-desk/services && rm -rf ~/signal-desk/infra/collector ~/signal-desk/services/collector"`
    - `scp -r E:/source/signal-desk-worktrees/collector-005/infra/collector signaldesk-pi:~/signal-desk/infra/`
    - `scp -r E:/source/signal-desk-worktrees/collector-005/services/collector signaldesk-pi:~/signal-desk/services/`
    - `ssh signaldesk-pi "cp ~/signal-desk/infra/collector/.env.example ~/signal-desk/infra/collector/.env && sed -i 's/^SIGNALDESK_COLLECTOR_NODE_ID=.*/SIGNALDESK_COLLECTOR_NODE_ID=collector-pi-192-168-0-33/' ~/signal-desk/infra/collector/.env"`
    - `ssh signaldesk-pi "docker compose -f ~/signal-desk/infra/collector/docker-compose.yml --env-file ~/signal-desk/infra/collector/.env build collector-bootstrap collector-runner collector-shipper"`
    - `ssh signaldesk-pi "docker compose -f ~/signal-desk/infra/collector/docker-compose.yml --env-file ~/signal-desk/infra/collector/.env down -v --remove-orphans"`
    - `ssh signaldesk-pi "docker compose -f ~/signal-desk/infra/collector/docker-compose.yml --env-file ~/signal-desk/infra/collector/.env up -d collector-db"`
    - `ssh signaldesk-pi "docker compose -f ~/signal-desk/infra/collector/docker-compose.yml --env-file ~/signal-desk/infra/collector/.env run --rm collector-bootstrap"`
    - `ssh signaldesk-pi "docker compose -f ~/signal-desk/infra/collector/docker-compose.yml --env-file ~/signal-desk/infra/collector/.env run --rm collector-runner"`
    - `ssh signaldesk-pi "docker compose -f ~/signal-desk/infra/collector/docker-compose.yml --env-file ~/signal-desk/infra/collector/.env run --rm -e SIGNALDESK_COLLECTOR_SHIPPER_MODE=simulate-intake -e SIGNALDESK_COLLECTOR_SIMULATED_INTAKE_STATUS=accepted -e SIGNALDESK_COLLECTOR_SIMULATED_REASON_CODE=accepted collector-shipper"`
    - `ssh signaldesk-pi "cat ~/signal-desk/infra/collector/queries/spool-evidence.sql | docker compose -f ~/signal-desk/infra/collector/docker-compose.yml --env-file ~/signal-desk/infra/collector/.env exec -T collector-db psql -U collector -d signaldesk_collector -f -"`
    - `ssh signaldesk-pi "docker compose -f ~/signal-desk/infra/collector/docker-compose.yml --env-file ~/signal-desk/infra/collector/.env run --rm collector-runner"`
    - `ssh signaldesk-pi "docker compose -f ~/signal-desk/infra/collector/docker-compose.yml --env-file ~/signal-desk/infra/collector/.env run --rm collector-shipper"`
    - `ssh signaldesk-pi "cat ~/signal-desk/infra/collector/queries/spool-idempotency.sql | docker compose -f ~/signal-desk/infra/collector/docker-compose.yml --env-file ~/signal-desk/infra/collector/.env exec -T collector-db psql -U collector -d signaldesk_collector -f -"`
    - `ssh signaldesk-pi "cat ~/signal-desk/infra/collector/queries/spool-quality-summary.sql | docker compose -f ~/signal-desk/infra/collector/docker-compose.yml --env-file ~/signal-desk/infra/collector/.env exec -T collector-db psql -U collector -d signaldesk_collector -f -"`
    - `ssh signaldesk-pi "docker compose -f ~/signal-desk/infra/collector/docker-compose.yml --env-file ~/signal-desk/infra/collector/.env down"`
  - `git diff --check`
- result:
  - pre-fix baseline defect:
    - local accepted-intake verification showed:
      - `status=accepted`
      - `last_intake_status=accepted`
      - `retry_count=1`
      - `transport_status=intake_response`
    - this was the wrong state for successful intake.
  - post-fix local accepted-intake verification:
    - `status=accepted`
    - `last_intake_status=accepted`
    - `retry_count=0`
    - `transport_status=delivered`
  - post-fix local idempotency/quality query:
    - `total_rows=2`
    - `distinct_payload_hashes=2`
    - `distinct_idempotency_keys=2`
    - `total_ingest_count=4`
    - `rows_with_retries=2`
    - `accepted_rows_with_retry_count=0`
    - `rows_with_missing_core_metadata=0`
  - post-fix Pi accepted-intake verification:
    - `transport_status=delivered`
    - `retry_count=0`
    - `language=en`, `market_scope=KRX`, normalized canonical URL values present
  - post-fix Pi duplicate+offline verification:
    - `rows_with_retries=2`
    - `accepted_rows_with_retry_count=0`
    - `rows_with_missing_core_metadata=0`
  - transient Pi runtime notes:
    - initial bootstrap immediately after DB up failed once with unhealthy dependency; rerun succeeded
    - one runner command timed out during container preparation; rerun with longer timeout succeeded
  - tests/checks:
    - `python -m unittest discover -s tests -v` passed (`4` tests)
    - `python -m compileall services/collector/signaldesk_collector` passed
    - `git diff --check` passed (CRLF warnings only)

## Readiness / Blockers
- local and Pi fixture smokes are both green after fixes
- remaining system blocker unchanged:
  - central intake endpoint/auth rollout is pending upstream backend lanes (shipper verified retryable-offline behavior and intake simulation behavior)

## Files Touched
- `infra/collector/README.md`
- `infra/collector/queries/spool-evidence.sql`
- `infra/collector/queries/spool-idempotency.sql`
- `infra/collector/queries/spool-quality-summary.sql`
- `services/collector/signaldesk_collector/main.py`
- `services/collector/tests/test_quality_rules.py`
- `coordination/tasks.yaml`
- `coordination/handoffs/COL-005.md`
- `coordination/resume/COL-005.md`
