# Handoff BE-001

## Task
- id: BE-001
- owner: signal-desk-backend
- status: in_progress (contracts refreshed on latest main; awaiting QA)

## What Changed
- updated `docs/backend/api-contract.md` to align with accepted DATA-001 scoring contracts:
  - added canonical `risk_flags`/`reason_tags` literal sets
  - documented API alias mapping (`score`->`score_total`, `delta_1d`->`score_delta_24h`)
  - added `is_alert_eligible` coverage across dashboard, ranking, detail, and watchlist payloads
  - tightened DATA-001 lock assumptions (snapshot key, precision, enum constraints)
- updated `docs/backend/db-schema.md` to match DATA-001 storage expectations:
  - `keyword_snapshots` now uses `as_of_ts` + unique (`keyword_id`,`as_of_ts`)
  - score fields use DATA precision (`numeric(5,2)` / `numeric(4,3)`)
  - dimensions renamed to `dimension_*` set and added `rank_position`, `is_alert_eligible`
  - `reason_tags`/`risk_flags` moved to `text[]` arrays with canonical-risk enforcement notes
- refreshed BE task handoff/resume content for current re-activation cycle

## Current State
- BE contracts are now synchronized to DATA-001 final docs and are suitable for downstream UX/APP/OPS planning.
- Task remains open pending QA review and orchestrator acceptance.

## Verification
- commands:
  - `Select-String -Path .\docs\backend\api-contract.md -Pattern 'GET /dashboard|GET /keywords|GET /keywords/{keyword_id}|GET /watchlist|POST /watchlist|GET /alerts'`
  - `Select-String -Path .\docs\backend\api-contract.md -Pattern 'canonical `risk_flags` literals|API field aliases|is_alert_eligible|DATA-001 Assumptions Locked For BE-001'`
  - `Select-String -Path .\docs\backend\db-schema.md -Pattern 'keyword_snapshots|as_of_ts|score_total|score_delta_24h|dimension_mentions|is_alert_eligible|risk_flags'`
  - `Select-String -Path .\docs\backend\db-schema.md -Pattern 'unique \(`keyword_id`,`as_of_ts`\)|canonical `risk_flags` values'`
  - `Select-String -Path .\docs\design\screen-map.md -Pattern 'Home|Keyword Ranking|Keyword Detail|Watchlist|Alerts'`
- result:
  - all required screen flows map to frozen API endpoints.
  - DB read model fields and constraints now explicitly match DATA-001 scoring contracts.

## Blockers
- none from backend docs side
- QA review still required before BE-001 acceptance

## Next Step
- QA reviews BE-001 docs and records verdict in `coordination/handoffs/BE-001-QA.md`.

## Files Touched
- docs/backend/api-contract.md
- docs/backend/db-schema.md
- coordination/handoffs/BE-001.md
- coordination/resume/BE-001.md
