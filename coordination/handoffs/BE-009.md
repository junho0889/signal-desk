## Task
- id: BE-009
- owner: signal-desk-backend
- status: done

## What Changed
- expanded `docs/backend/collector-intake-contract.md` to freeze central intake validation and quarantine behavior:
  - endpoint and full request envelope fields
  - item outcome classes: `accepted`, `accepted_degraded`, `duplicate`, `quarantined`, `rejected`, `retryable_failure`
  - validation stage order and explicit metadata rules for reject, downgrade, quarantine, duplicate, and retryable outcomes
  - batch and item response contract with `quality_state`, `reason_code`, and `retryable` fields
  - replay/resubmission semantics for quarantined and rejected payloads
  - explicit mapping from intake outcomes to storage quality states
- updated `docs/backend/api-contract.md` internal collector intake section to match the same frozen outcomes and item-level auditability requirements

## Validation And Response Behaviors Frozen For Implementation/QA
- item-level validation decisions are mandatory and cannot be inferred from only top-level batch status
- non-accepted items require explicit non-generic `reason_code`
- quarantine is a persisted, replayable state (not an opaque failure)
- downgrade (`accepted_degraded`) is distinct from reject and remains API/audit visible
- idempotent duplicate behavior is explicit and must not create duplicate canonical raw rows

## Consistency Review Summary
- aligned with `docs/data/collector-metadata-quality-v1.md` required metadata and quality-state vocabulary
- aligned with `docs/backend/db-schema.md`/storage-lane boundary by freezing state mapping while leaving physical table names to BE-008
- aligned with BE-009 dispatch constraints on replayability and item-level auditability

## Verification
- commands:
  - `git -C E:\source\signal-desk-worktrees\be-009 diff --check`
- result:
  - passed (no diff-check errors)

## Open Blockers / Missing Upstream Handoffs
- `coordination/handoffs/BE-005.md` not present in this worktree
- `coordination/handoffs/BE-008.md` not present in this worktree
- `coordination/handoffs/COL-004.md` not present in this worktree

## Next Step
- collector, storage, and QA lanes can implement and test against the frozen item-level statuses, reason codes, and quarantine/replay behavior defined here
