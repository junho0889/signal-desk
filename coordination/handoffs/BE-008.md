## Task
- id: BE-008
- owner: signal-desk-storage
- status: done

## What Changed
- claimed `BE-008` in `coordination/tasks.yaml`, then marked it `done` for integration review.
- expanded `docs/backend/storage-expansion-outline.md` with frozen raw-ingest table families:
  - source registry
  - collector spool lineage
  - central intake persistence
  - metadata quality persistence
- expanded `docs/backend/db-schema.md` with additive BE-008 schema contracts for:
  - source registry and contract versions
  - spool runs/items and intake request lineage
  - central raw metadata persistence columns (not opaque-only metadata)
  - quality state current/history, duplicates, quarantine, and dead-letter records
- added `docs/data/collector-metadata-quality-v1.md` in this worktree with required fields, state rules, storage mapping, and replay guarantees.

## Integration Review Scope (Locked)
- in scope:
  - raw-ingest metadata schema
  - quality-state persistence and transitions
  - spool-to-central raw lineage required for metadata replay/audit
- out of scope:
  - normalization, trust, model, and publish-layer contracts
  - intake API path/auth details
  - runtime implementation code

## Consistency Review
- reviewed against:
  - `docs/backend/collector-intake-contract.md`
  - `docs/data/source-catalog.md`
  - `docs/data/collector-metadata-quality-v1.md`
- result:
  - collector envelope fields (`source_id`, `collected_at`, `upstream_event_at`, `payload_hash`, `payload_version`, `collector_node_id`, `retry_count`) are persisted in frozen metadata contract tables.
  - quality states are persistable and replayable with explicit lineage and transition history.

## Frozen Contracts For Collector And Backend Lanes
- metadata fields frozen for persistence:
  - `source_id`
  - `source_category`
  - `collector_node_id`
  - `spool_item_key`
  - `retry_count`
  - `idempotency_key`
  - `payload_hash`
  - `payload_version`
  - `collected_at`
  - `upstream_event_at`
  - `publisher_name`
  - `publisher_domain`
  - `canonical_url`
  - `external_id`
  - `language`
  - `market_scope`
  - `title`
  - `raw_payload_json`
- quality-state persistence tables frozen:
  - `raw_item_quality_states`
  - `raw_item_quality_history`
  - `raw_duplicate_links`
  - `raw_quarantine_records`
  - `raw_dead_letter_records`
- spool-to-central lineage tables frozen:
  - `collector_spool_items`
  - `intake_requests`
  - `raw_item_spool_links`
  - `raw_items`

## Integration Review Checklist
- required metadata fields are persisted as explicit columns (not only JSON blob).
- quality-state enum and transition persistence are explicit and queryable.
- duplicate/quarantine/dead-letter behavior is represented with dedicated tables.
- replay lineage from collector spool item to central raw row is explicit.

## Verification
- commands:
  - `git -C E:\source\signal-desk-worktrees\storage-003 diff -- coordination/tasks.yaml docs/backend/storage-expansion-outline.md docs/backend/db-schema.md docs/data/collector-metadata-quality-v1.md`
  - `git -C E:\source\signal-desk-worktrees\storage-003 diff --check`
- result:
  - BE-008 scope diff is isolated to owned files and task/handoff/resume coordination.
  - `git diff --check` passed with no whitespace errors.

## Review Mode
- per latest operator direction, this BE-008 checkpoint is treated as contract-freeze work for orchestrator review only (QA wait is intentionally skipped for this step).

## Open Blockers
- none for contract freeze drafting; downstream implementation depends on orchestrator acceptance and BE-009/COL follow-on execution.

## Next Step
- orchestrator integration review of BE-008 contract freeze.
- BE-009 and collector lanes implement against this frozen metadata + quality-state contract after review acceptance.

## Files Touched
- `coordination/tasks.yaml`
- `docs/backend/storage-expansion-outline.md`
- `docs/backend/db-schema.md`
- `docs/data/collector-metadata-quality-v1.md`
- `coordination/handoffs/BE-008.md`
- `coordination/resume/BE-008.md`
