## Task
- id: OPS-002
- owner: signal-desk-ops
- status: in_progress (operational baseline complete; awaiting QA/orchestrator acceptance)

## What Changed
- expanded local compose runtime to include `postgres`, `api`, and `jobs` services in `infra/local/docker-compose.yml`.
- added jobs/api operational env knobs in `infra/local/.env.example`:
  - `JOBS_INTERVAL_SECONDS`
  - `SIGNALDESK_ALERT_DELTA_THRESHOLD`
- introduced local bootstrap init mount under `infra/local/postgres-init/` and added `001-bootstrap.sh` with LF-only encoding.
- updated `docs/ops/deploy-runbook.md` to reflect deterministic bring-up/tear-down, health checks, backup/restore, and rollback for full stack.
- added `docs/ops/operations-checklist.md` with step-by-step operational checklist.

## Current State
- full local stack (`postgres`, `api`, `jobs`) boots and reports healthy with compose.
- contract smoke and freshness checks pass against running local stack.

## Verification
- commands:
  - `docker compose -f infra/local/docker-compose.yml --env-file infra/local/.env config`
  - `docker compose -f infra/local/docker-compose.yml --env-file infra/local/.env down -v`
  - `docker compose -f infra/local/docker-compose.yml --env-file infra/local/.env up -d --build`
  - `docker compose -f infra/local/docker-compose.yml --env-file infra/local/.env ps`
  - `docker compose -f infra/local/docker-compose.yml --env-file infra/local/.env logs postgres --tail=80`
  - `docker compose -f infra/local/docker-compose.yml --env-file infra/local/.env logs jobs --tail=120`
  - `Invoke-RestMethod 'http://127.0.0.1:8000/healthz'`
  - `Invoke-RestMethod 'http://127.0.0.1:8000/v1/dashboard'`
  - `Invoke-RestMethod 'http://127.0.0.1:8000/v1/keywords?period=daily&market=all&limit=20'`
  - `Invoke-RestMethod 'http://127.0.0.1:8000/v1/watchlist'`
  - `Invoke-RestMethod 'http://127.0.0.1:8000/v1/alerts?limit=20'`
  - `docker exec signaldesk-postgres psql -U postgres -d signaldesk -c "SELECT rolname FROM pg_roles WHERE rolname IN ('signaldesk_migrator','signaldesk_app','signaldesk_readonly') ORDER BY rolname;"`
  - `docker exec signaldesk-postgres psql -U postgres -d signaldesk -c "SELECT now() AT TIME ZONE 'utc' AS now_utc, MAX(as_of_ts) AS latest_snapshot_utc FROM keyword_snapshots;"`
  - `docker compose -f infra/local/docker-compose.yml --env-file infra/local/.env restart api jobs`
  - `docker compose -f infra/local/docker-compose.yml --env-file infra/local/.env ps`
- result:
  - compose config renders successfully.
  - all three services reach `healthy` state.
  - postgres init script executes successfully from `infra/local/postgres-init` (roles and grants created).
  - API health and core contract endpoints respond as expected.
  - role presence and snapshot freshness checks pass.
  - `api`/`jobs` restart completes with services returning to `healthy`.

## Blockers
- none.

## Next Step
- QA reviews OPS-002 runtime changes and publishes `coordination/handoffs/OPS-002-QA.md` verdict.

## Files Touched
- infra/local/docker-compose.yml
- infra/local/.env.example
- infra/local/postgres-init/001-bootstrap.sh
- docs/ops/deploy-runbook.md
- docs/ops/operations-checklist.md
- coordination/handoffs/OPS-002.md
