## Task
- id: BE-004
- owner: signal-desk-storage
- status: in_progress

## What Changed
- expanded `docs/backend/storage-expansion-outline.md` with a five-layer storage model: raw, normalized, trust, model, and published ranking.
- defined required table families per layer, lineage anchors, retention baselines, and least-privilege runtime boundaries.
- expanded `docs/backend/db-schema.md` with additive BE-004 schema contracts for:
  - raw ingestion runs and immutable raw items
  - normalization runs, canonical events, dedup clusters, entity mappings, contradiction edges
  - trust runs and per-event/per-keyword trust outputs
  - feature/model runs, model registry, model outputs, run metrics
  - publish runs, immutable published rankings, and ranking evidence links
- documented raw-to-app lineage rules and serving projection lineage fields for `keyword_snapshots`.

## Consistency Review
- reviewed BE-004 storage layering against:
  - `docs/architecture/intelligence-platform-topology.md` (collector -> central storage -> normalization/trust/model -> API read model flow)
  - `docs/backend/postgres-security.md` (least-privilege, no superuser runtime posture)
- result: design stays additive to BE-001 API-facing tables and keeps privilege separation explicit by processing boundary.

## Frozen Contracts For Downstream Lanes
- TRUST-001 should treat these outputs as storage contract targets:
  - `trust_runs`
  - `event_trust_assessments`
  - `keyword_trust_snapshots`
- MODEL-001 should treat these outputs as storage contract targets:
  - `feature_runs`
  - `keyword_feature_snapshots`
  - `model_runs`
  - `model_keyword_scores`
  - `model_run_metrics`
- publish boundary and app projection freeze:
  - `publish_runs`
  - `published_keyword_rankings`
  - `published_ranking_evidence`
  - `keyword_snapshots.publish_run_id`
  - `keyword_snapshots.published_ranking_id`

## Verification
- commands:
  - `git -C E:\source\signal-desk-worktrees\storage-001 diff -- docs/backend/storage-expansion-outline.md`
  - `git -C E:\source\signal-desk-worktrees\storage-001 diff -- docs/backend/db-schema.md`
  - `git -C E:\source\signal-desk-worktrees\storage-001 diff --check`
- result:
  - diffs reflect BE-004 scope only (tasks + storage docs + coordination notes)
  - `git diff --check` passed with no whitespace/conflict errors

## Open Blockers
- `coordination/handoffs/COL-001.md` is still missing, so intake-facing details remain additive but not fully frozen against collector spool specifics.

## Next Step
- once COL-001 handoff exists, reconcile any collector-specific intake fields and then move BE-004 to done.

## Files Touched
- `coordination/tasks.yaml`
- `docs/backend/storage-expansion-outline.md`
- `docs/backend/db-schema.md`
- `coordination/handoffs/BE-004.md`
- `coordination/resume/BE-004.md`
