## Task
- id: APP-003
- owner: signal-desk-mobile
- status: done

## What Changed
- added a shared `SignalDeskRepository` layer above the typed API client
- added reusable `LoadableController` and `LoadableView` primitives for loading, error, empty, and retry states
- migrated Home, Ranking, Detail, Watchlist, and Alerts away from ad hoc `FutureBuilder` usage
- updated mobile implementation notes to reflect the new state and verification expectations

## Current State
- all core MVP screens now use the same repository-backed async state pattern
- filter changes on Ranking and Alerts trigger explicit refreshes instead of rebuilding unmanaged futures
- watchlist mutation failures now surface explicit snackbar feedback in Detail

## Verification
- commands:
  - `Get-ChildItem app/mobile/lib -Recurse -Filter *.dart | Select-String -Pattern 'FutureBuilder<'`
  - `Get-ChildItem app/mobile/lib -Recurse -Filter *.dart | Select-String -Pattern 'LoadableView<|LoadableController<|SignalDeskRepository'`
  - `flutter --version`
- result:
  - no `FutureBuilder` usages remain under `app/mobile/lib`
  - repository and shared state abstractions are referenced by all core feature screens
  - Flutter SDK is unavailable in this session, so `flutter analyze`, `flutter test`, and runtime smoke are blocked

## Blockers
- Flutter SDK and Android toolchain are not available in this environment for runtime verification

## Next Step
- run `QA-002` review for `OPS-003` and `APP-003`
- complete Flutter-based verification on a machine with the SDK installed before release candidate sign-off

## Files Touched
- `app/mobile/lib/src/app.dart`
- `app/mobile/lib/core/repositories/signaldesk_repository.dart`
- `app/mobile/lib/core/state/loadable_controller.dart`
- `app/mobile/lib/features/shared/loadable_view.dart`
- `app/mobile/lib/features/home/home_screen.dart`
- `app/mobile/lib/features/ranking/ranking_screen.dart`
- `app/mobile/lib/features/detail/keyword_detail_screen.dart`
- `app/mobile/lib/features/watchlist/watchlist_screen.dart`
- `app/mobile/lib/features/alerts/alerts_screen.dart`
- `docs/mobile/implementation-notes.md`
