## Task
- id: OPS-006
- owner: signal-desk-ops
- status: done

## What Changed
- updated `docs/ops/local-docker-stack.md` to mirror the exact `COL-003` collector smoke surface under `infra/collector/`
- updated `docs/ops/deploy-runbook.md` with the exact local collector test-db commands from `COL-003` plus the minimal promotion path for Raspberry Pi `192.168.0.33`
- updated `docs/ops/quality-gates.md` with the concrete collector verification gate tied to `collector-bootstrap`, `collector-runner`, `collector-shipper`, and the shipped SQL queries
- added the missing OPS-006 dispatch file to this worktree

## Current State
- `COL-003` is reviewable at `c46b036` in `E:\source\signal-desk-worktrees\collector-003`
- OPS-006 now freezes the exact local smoke path around the real collector assets and commands already present in `collector-003`
- the Raspberry Pi promotion path is intentionally minimal: keep the same compose file, entrypoints, and SQL inspection surface; change only node identity, host-local env, and later central-target settings
- no Windows system settings are required or changed for this workflow

## Integration Review Focus
- review only the local collector smoke path in `docs/ops/local-docker-stack.md` and `docs/ops/deploy-runbook.md`
- review only the Raspberry Pi promotion steps for `192.168.0.33` in `docs/ops/deploy-runbook.md`
- confirm `docs/ops/quality-gates.md` requires the same concrete command surface and SQL evidence as `COL-003`
- do not expand review scope into central deploy, auth rollout, or non-collector app stack changes

## Verification
- commands:
  - `Get-Content -Raw docs/ops/local-docker-stack.md`
  - `Get-Content -Raw docs/ops/deploy-runbook.md`
  - `Get-Content -Raw docs/ops/quality-gates.md`
  - `Get-Content -Raw E:\source\signal-desk-worktrees\collector-003\infra\collector\docker-compose.yml`
  - `Get-Content -Raw E:\source\signal-desk-worktrees\collector-003\infra\collector\.env.example`
  - `Get-Content -Raw E:\source\signal-desk-worktrees\collector-003\coordination\handoffs\COL-003.md`
  - `git -C E:\source\signal-desk-worktrees\collector-003 log --oneline --decorate -n 3`
  - `Select-String -Path docs/ops/local-docker-stack.md,docs/ops/deploy-runbook.md,docs/ops/quality-gates.md,E:\source\signal-desk-worktrees\collector-003\infra\collector\docker-compose.yml,E:\source\signal-desk-worktrees\collector-003\infra\collector\.env.example,E:\source\signal-desk-worktrees\collector-003\coordination\handoffs\COL-003.md -Pattern 'collector-bootstrap|collector-runner|collector-shipper|spool-evidence.sql|spool-idempotency.sql|192.168.0.33|collector-pi-192-168-0-33' | Select-Object Path, LineNumber, Line`
  - `git diff --check`
- result:
  - OPS-006 docs now match the reviewable `COL-003` command surface instead of the earlier speculative placeholders
  - the local workflow and the Pi promotion path are command-compatible by design
  - `git diff --check` passed apart from line-ending warnings on edited Markdown files

## Open Blockers
- collector assets are still not present in the OPS-006 worktree itself, so runtime execution from this branch is not possible without pulling from the collector branch or waiting for merge/integration
- collector deployment on Raspberry Pi `192.168.0.33` still depends on a later session with Docker available on the Pi and central-target env values frozen by backend work

## Next Step
- once collector assets are integrated beyond `collector-003`, rerun the exact local smoke commands from the OPS docs and record direct runtime output from the active worktree
- when the Raspberry Pi session is ready, use the frozen Pi path in `docs/ops/deploy-runbook.md` and record exact commands plus blockers against `192.168.0.33`

## Files Touched
- `coordination/dispatches/OPS-006.md`
- `coordination/handoffs/OPS-006.md`
- `docs/ops/local-docker-stack.md`
- `docs/ops/deploy-runbook.md`
- `docs/ops/quality-gates.md`
