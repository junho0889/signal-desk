## Task
- id: OPS-003
- owner: signal-desk-ops
- status: done

## What Changed
- split the old recurring `run-once` loop into a one-time `jobs-bootstrap` service and a long-running `jobs` service
- updated the runtime docs so bootstrap verification uses `docker compose ... ps -a jobs-bootstrap`
- documented restart and re-bootstrap procedures separately from normal runtime restarts

## Current State
- `jobs-bootstrap` now runs `migrate`, `seed-demo`, and initial `evaluate-alerts` once, then exits successfully
- `jobs` now loops only `evaluate-alerts`, which removes repeated migration and demo seed work from steady-state runtime
- local stack verification passed with healthy `postgres`, `api`, and `jobs`

## Verification
- commands:
  - `docker compose -f .\infra\local\docker-compose.yml --env-file .\infra\local\.env config`
  - `docker compose -f .\infra\local\docker-compose.yml --env-file .\infra\local\.env down`
  - `docker compose -f .\infra\local\docker-compose.yml --env-file .\infra\local\.env up -d --build`
  - `docker compose -f .\infra\local\docker-compose.yml --env-file .\infra\local\.env ps -a jobs-bootstrap`
  - `docker compose -f .\infra\local\docker-compose.yml --env-file .\infra\local\.env logs jobs-bootstrap --tail 80`
  - `docker compose -f .\infra\local\docker-compose.yml --env-file .\infra\local\.env logs jobs --tail 80`
  - `docker compose -f .\infra\local\docker-compose.yml --env-file .\infra\local\.env ps`
  - `Invoke-RestMethod "http://127.0.0.1:8000/healthz"`
  - `Invoke-RestMethod "http://127.0.0.1:8000/v1/dashboard" | ConvertTo-Json -Depth 4`
- result:
  - compose config validated successfully
  - `jobs-bootstrap` exited with code `0`
  - `jobs-bootstrap` logs showed one-time `migrate`, `seed-demo`, and `evaluate-alerts`
  - `jobs` logs showed only recurring `evaluate-alerts`
  - `postgres`, `api`, and `jobs` reached healthy state and API smoke checks passed

## Blockers
- none for ops scope

## Next Step
- mark `OPS-003` done and proceed to `APP-003`
- include this ops change in `QA-002` regression review after mobile hardening lands

## Files Touched
- `infra/local/docker-compose.yml`
- `docs/ops/deploy-runbook.md`
- `docs/ops/operations-checklist.md`
