## Task
- id: BE-002
- owner: signal-desk-backend
- status: in_progress (implementation baseline complete; awaiting QA/orchestrator acceptance)

## What Changed
- implemented backend API service under `services/api/` (FastAPI + psycopg):
  - `GET /v1/dashboard`
  - `GET /v1/keywords`
  - `GET /v1/keywords/{keyword_id}`
  - `GET /v1/watchlist`
  - `POST /v1/watchlist`
  - `GET /v1/alerts`
  - `GET /healthz`
- added contract-aligned API error handling (`invalid_argument`, `not_found`, `internal_error`) with BE-001 error shape.
- implemented jobs runtime under `services/jobs/`:
  - SQL migration runner
  - demo data seeding path
  - watchlist alert evaluation path
- added initial schema migration `services/jobs/migrations/001_schema.sql` aligned to `docs/backend/db-schema.md` (tables/indexes/check constraints/grants).
- added backend implementation run/verify notes in `docs/backend/implementation-notes.md`.

## Current State
- BE-002 code and docs are implemented in this branch.
- docker-backed local verification ran successfully for migration, seed, alert evaluation, and `/v1` endpoint responses.

## Verification
- commands:
  - `python -m compileall services\api\app services\jobs\signaldesk_jobs`
  - `Copy-Item infra\local\.env.example infra\local\.env`
  - `docker compose -f infra/local/docker-compose.yml up -d postgres`
  - `docker compose -f infra/local/docker-compose.yml ps`
  - `docker compose -f infra/local/docker-compose.yml logs postgres --tail=60`
  - `pip install -r services/api/requirements.txt -r services/jobs/requirements.txt`
  - `$env:SIGNALDESK_MIGRATOR_DATABASE_URL='postgresql://signaldesk_migrator:change-this-migrator-password@127.0.0.1:5432/signaldesk'; $env:SIGNALDESK_APP_DATABASE_URL='postgresql://signaldesk_app:change-this-app-password@127.0.0.1:5432/signaldesk'; python -m services.jobs.signaldesk_jobs.main migrate; python -m services.jobs.signaldesk_jobs.main seed-demo; python -m services.jobs.signaldesk_jobs.main evaluate-alerts`
  - inline Python smoke for API endpoints (`/v1/dashboard`, `/v1/keywords`, `/v1/keywords/{id}`, `/v1/watchlist`, `/v1/alerts`, `POST /v1/watchlist`) against running uvicorn
  - inline Python validation for error shape (`GET /v1/keywords?period=hourly`)
- result:
  - compile checks passed.
  - migrations applied (`001_schema.sql`), demo data seeded, and alert job inserted expected alerts.
  - API endpoint smoke results:
    - `DASHBOARD_TOP=3`
    - `KEYWORDS_COUNT=3`
    - `DETAIL_KEYWORD=AI Infrastructure`
    - `WATCHLIST_KEYWORDS=1`
    - `WATCHLIST_STOCKS=1`
    - `ALERTS_COUNT=2`
    - watchlist mutation returned `ok=true`.
  - error shape check returned `status=400`, `error.code=invalid_argument`, `details.field=period`.

## Blockers
- `infra/postgres/init/001-bootstrap.sh` currently fails inside container due BOM/CRLF line-ending issue in this environment.
- workaround used for verification: one-time manual SQL bootstrap of roles/database/schema privileges inside `signaldesk-postgres`.

## Next Step
- QA reviews `worker/be-002`, writes `coordination/handoffs/BE-002-QA.md`, and records verdict (`pass`/`pass-with-notes`/`fail`).

## Files Touched
- services/api/
- services/jobs/
- docs/backend/implementation-notes.md
- coordination/handoffs/BE-002.md
