## Task
- id: BE-005
- owner: signal-desk-backend
- status: done

## What Changed
- expanded `docs/backend/collector-intake-contract.md` from baseline notes into a concrete internal intake contract:
  - endpoint path and host target (`192.168.0.200`)
  - required batch and item envelope fields
  - acknowledgement classes (`accepted|duplicate|rejected|retryable_failure`)
  - item and batch idempotency keys
  - partial-success handling rules
  - retryable vs non-retryable backend outcome policy
  - backend/storage coordination fields for raw persistence
- added matching `Internal Collector Intake API` section to `docs/backend/api-contract.md` so the internal endpoint shape and semantics are visible with other contracts

## Current State
- central collector intake contract is now explicit enough for collector shipping and storage-lane alignment
- contract keeps storage implementation details open where BE-004/COL-002 are still pending

## Verification
- commands:
  - `git -C E:\source\signal-desk-worktrees\be-005 diff --check`
- result:
  - passed (no diff-check errors)

## Open Blockers / Open Coordination
- `coordination/handoffs/COL-002.md` is not present yet, so collector-side implementation assumptions remain partially open
- final auth mechanism and concrete storage table naming stay intentionally unfrozen pending storage/collector alignment

## Next Step
- collector (`COL-002`) and storage (`BE-004`) lanes should confirm:
  - practical batch-size limits and shipper defaults
  - auth rollout mechanism for collector-to-central intake
  - final raw-ingest table/schema naming and retention implementation
