## Task
- id: COL-004
- owner: signal-desk-collector
- status: done

## What Changed
- froze collector metadata-first quality overlay in `docs/data/source-catalog.md`
- expanded `docs/data/collector-metadata-quality-v1.md` into a deterministic contract with:
  - frozen envelope fields
  - source-family idempotency rules
  - explicit quality states and ingest status enums
  - ordered non-AI rule execution model
  - explicit reject, downgrade, duplicate, stale, and quarantine rules
  - adapter evidence requirements and downstream frozen field list
- documented release-gate note that orchestrator review is the active gate until broader runnable collector output exists

## Current State
- collector quality handling is now explicit enough for adapter code and DB constraints
- storage (`BE-008`), backend (`BE-009`), and QA (`QA-007`) can consume one frozen metadata/quality vocabulary
- task status for `COL-004` is set to `done`

## Verification
- commands:
  - `Get-ChildItem -Path docs/data,docs/backend,docs/ops -Recurse -File | Select-String -Pattern 'quality_state|ingest_status|dead_letter|quarantined|duplicate|stale_source|idempotency_key|payload_hash|collector_node_id|retryable_failure'`
  - `git diff --check`
- result:
  - consistency scan confirmed shared terminology alignment across collector data docs, backend intake contract, and collector runtime doc
  - `git diff --check` passed

## Frozen Metadata And Quality Fields
- `source_id`
- `source_category`
- `collector_node_id`
- `collected_at`
- `upstream_event_at`
- `publisher_name`
- `publisher_domain`
- `canonical_url`
- `payload_hash`
- `payload_version`
- `idempotency_key`
- `quality_state`
- `ingest_status`
- `retry_count`
- `last_error_code`
- `adapter_version`
- `retrieval_status`

## Blockers
- `coordination/handoffs/COL-003.md` and `coordination/handoffs/BE-008.md` were not present in this worktree snapshot during authoring

## Next Step
- storage lane (`BE-008`) should map DB schema columns and constraints directly to the frozen fields/states
- backend lane (`BE-009`) should map intake validation outcomes to `quality_state` + `ingest_status` without introducing alternate state labels
- QA lane (`QA-007`) should validate fixture evidence against these exact rule outcomes

## Files Touched
- `docs/data/source-catalog.md`
- `docs/data/collector-metadata-quality-v1.md`
- `coordination/tasks.yaml`
- `coordination/handoffs/COL-004.md`
- `coordination/resume/COL-004.md`
