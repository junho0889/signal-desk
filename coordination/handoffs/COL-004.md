## Task
- id: COL-004
- owner: signal-desk-collector
- status: done

## What Changed
- refreshed `docs/data/collector-metadata-quality-v1.md` with recent spool evidence metrics from collector test-db queries.
- added a prioritized, implementation-oriented `TOP 5 FIXES` section with measurable thresholds and exact collector rule changes.
- narrowed updates to metadata schema and quality-state persistence behavior only.

## Recent Spool Evidence (collector-003 local test-db, 2026-03-09)
- run sequence:
  - bootstrap DB
  - ingest fixture run #1 (`inserted=2`)
  - ingest fixture run #2 (`upserted=2`)
  - shipper simulate-offline run (`touched_rows=2`)
- measured outcomes:
  - `spool_rows=2`
  - `total_ingest_events=4`
  - `metadata_completeness_pct=100.00`
  - `duplicate_ratio_pct=50.00`
  - `quality_state_duplicate_pct=100.00`
  - `stale_ratio_pct=0.00`
  - `missing_required_fields_ratio_pct=0.00`
  - `blank_source_cursor_pct=100.00`
  - `publisher_domain_valid_pct=100.00`
  - shipper retry evidence: all rows `retry_count=1`, `last_intake_status=retryable_failure`

## TOP 5 FIXES
1. Preserve canonical `quality_state` on idempotent reruns; use counters/reason codes for duplicate hits instead of rewriting canonical rows.
2. Enforce source-family cursor policy (`requires_cursor`): hard-fail missing cursor for cursor-driven feeds; emit `not_applicable` for non-cursor feeds.
3. Enforce strict domain-host validation with quarantine on mismatch (`reason_code=publisher_domain_mismatch`).
4. Add source-family stale thresholds and stale fixture coverage; persist `stale_threshold_exceeded` reason code.
5. Persist explicit missing-field arrays and enforce hard reject on missing required fields; degrade only for recommended-field gaps.

## Consistency Review
- aligned with:
  - `docs/data/source-catalog.md`
  - `docs/backend/collector-intake-contract.md`
  - collector spool evidence schema from `services/collector` + local test-db query results
- result:
  - metadata-first quality contract now reflects observed spool behavior and closes concrete implementation gaps with measurable acceptance thresholds.

## Verification
- commands:
  - `docker compose -f infra/collector/docker-compose.yml --env-file infra/collector/.env.example down -v --remove-orphans`
  - `docker compose -f infra/collector/docker-compose.yml --env-file infra/collector/.env.example up -d collector-db`
  - `docker compose -f infra/collector/docker-compose.yml --env-file infra/collector/.env.example run --rm collector-bootstrap`
  - `docker compose -f infra/collector/docker-compose.yml --env-file infra/collector/.env.example run --rm collector-runner`
  - `docker compose -f infra/collector/docker-compose.yml --env-file infra/collector/.env.example run --rm collector-runner`
  - `docker compose -f infra/collector/docker-compose.yml --env-file infra/collector/.env.example run --rm collector-shipper`
  - ad hoc `psql` queries against `spool_items` for completeness, duplicate ratio, stale ratio, missing fields, and publisher/domain validity
  - `git diff --check`
- result:
  - evidence metrics captured and incorporated in doc updates.
  - `git diff --check` passed.

## Next Step
- collector implementation should apply the five rule changes directly in adapter envelope + upsert logic.
- backend intake (`BE-009`) should reuse the same reason codes and quality-state transitions.
- QA (`QA-007`) should assert thresholds from the updated `TOP 5 FIXES` section.

## Files Touched
- `docs/data/collector-metadata-quality-v1.md`
- `coordination/tasks.yaml`
- `coordination/handoffs/COL-004.md`
- `coordination/resume/COL-004.md`
