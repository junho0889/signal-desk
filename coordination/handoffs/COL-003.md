## Task
- id: COL-003
- owner: signal-desk-collector
- status: done

## What Changed
- added isolated collector dev stack under `infra/collector/` with:
  - `collector-db` (local PostgreSQL spool DB)
  - `collector-bootstrap` (schema bootstrap)
  - `collector-runner` (fixture ingest)
  - `collector-shipper` (offline shipping simulation)
  - optional `collector-monitor` (metrics snapshot)
- added collector service implementation under `services/collector/` with CLI commands:
  - `migrate`
  - `reset-db`
  - `ingest-fixture`
  - `ship-once`
  - `metrics`
- implemented fixture-backed ingest path (`news_primary`) writing spool metadata and state fields into local DB
- added reusable SQL queries for spool evidence and idempotency checks
- updated `docs/ops/collector-runtime-v1.md` with exact bootstrap, ingest, query, restart, and reset commands

## Current State
- collector stack can be started independently from the main app stack
- fixture ingest is repeatable and idempotent by `idempotency_key`
- central host offline behavior is represented by simulated shipper retries (`last_error_code=central_offline_simulated`)

## Verification
- commands:
  - `docker compose -f infra/collector/docker-compose.yml --env-file infra/collector/.env.example down -v --remove-orphans`
  - `docker compose -f infra/collector/docker-compose.yml --env-file infra/collector/.env.example up -d collector-db`
  - `docker compose -f infra/collector/docker-compose.yml --env-file infra/collector/.env.example run --rm collector-bootstrap`
  - `docker compose -f infra/collector/docker-compose.yml --env-file infra/collector/.env.example run --rm collector-runner`
  - `Get-Content -Raw 'infra/collector/queries/spool-evidence.sql' | docker compose -f infra/collector/docker-compose.yml --env-file infra/collector/.env.example exec -T collector-db psql -U collector -d signaldesk_collector -f -`
  - `docker compose -f infra/collector/docker-compose.yml --env-file infra/collector/.env.example run --rm collector-shipper`
  - `docker compose -f infra/collector/docker-compose.yml --env-file infra/collector/.env.example restart collector-db`
  - `docker compose -f infra/collector/docker-compose.yml --env-file infra/collector/.env.example run --rm collector-runner`
  - `Get-Content -Raw 'infra/collector/queries/spool-idempotency.sql' | docker compose -f infra/collector/docker-compose.yml --env-file infra/collector/.env.example exec -T collector-db psql -U collector -d signaldesk_collector -f -`
  - `python -m compileall services/collector/signaldesk_collector`
  - `git diff --check`
  - `docker compose -f infra/collector/docker-compose.yml --env-file infra/collector/.env.example down`
- result:
  - bootstrap and fixture ingest succeeded (`rows_read=2`, first run `inserted=2`, rerun `inserted=0/upserted=2`)
  - spool evidence rows persisted with required metadata and state fields
  - shipper simulation updated retry metadata (`retry_count=1`, `last_error_code=central_offline_simulated`)
  - idempotency query showed `total_rows=2`, `distinct_idempotency_keys=2`, `total_ingest_count=4`
  - Python compile and git diff checks passed

## Observed Spool Evidence
- row count: `2`
- status after shipper simulation: both rows `pending`
- quality state: both rows `accepted`
- retry metadata: both rows `retry_count=1`, `last_error_code=central_offline_simulated`
- idempotent rerun evidence: `ingest_count` increased to `2` per row without increasing row count

## Blockers
- `coordination/handoffs/COL-002.md` and `coordination/handoffs/OPS-005.md` were not available in this worktree
- `BE-005` and `OPS-005` remain pending, so intake endpoint-level integration is still deferred

## Next Step
- proceed to `COL-004` metadata-quality freeze using this runnable ingest baseline
- backend (`BE-008`/`BE-009`) can now map quality-state persistence and validation behavior against real spool fields

## Files Touched
- `infra/collector/docker-compose.yml`
- `infra/collector/.env.example`
- `infra/collector/README.md`
- `infra/collector/reset-test-db.ps1`
- `infra/collector/queries/spool-evidence.sql`
- `infra/collector/queries/spool-idempotency.sql`
- `services/collector/Dockerfile`
- `services/collector/requirements.txt`
- `services/collector/signaldesk_collector/__init__.py`
- `services/collector/signaldesk_collector/config.py`
- `services/collector/signaldesk_collector/db.py`
- `services/collector/signaldesk_collector/main.py`
- `services/collector/signaldesk_collector/fixtures/news_primary.json`
- `docs/ops/collector-runtime-v1.md`
- `coordination/tasks.yaml`
- `coordination/handoffs/COL-003.md`
- `coordination/resume/COL-003.md`
