## Task
- id: OPS-005
- owner: signal-desk-ops
- status: done

## What Changed
- updated `docs/ops/local-docker-stack.md` to define a separate collector Compose project on this PC, kept isolated from the main app stack
- updated `docs/ops/deploy-runbook.md` with a planned collector startup, verification, retention, and restart flow
- documented the promotion path from this PC to Ubuntu on Raspberry Pi 4B 8GB
- recorded the central-host assumption that collector delivery targets `192.168.0.200`

## Current State
- collector runtime guidance is now separated cleanly from the main app stack docs
- the docs define planned collector services: `collector-db`, `collector-runner`, `collector-shipper`, and optional `collector-monitor`
- restart behavior, retention, named-volume expectations, and network separation are explicit
- the runbook now distinguishes current-PC development from later Ubuntu-on-Pi deployment

## Verification
- commands:
  - `Get-Content -Raw docs/ops/collector-runtime-v1.md`
  - `Get-Content -Raw docs/ops/local-docker-stack.md`
  - `Get-Content -Raw docs/ops/deploy-runbook.md`
  - `Get-Content -Raw docs/ops/pi-collector-node.md`
  - `Get-Content -Raw docs/backend/collector-intake-contract.md`
  - `Select-String -Path docs/ops/local-docker-stack.md,docs/ops/deploy-runbook.md,docs/ops/collector-runtime-v1.md,docs/ops/pi-collector-node.md,docs/backend/collector-intake-contract.md -Pattern '192.168.0.200|collector-db|collector-runner|collector-shipper|30 days|Ubuntu|Raspberry Pi 4B 8GB|signaldesk-collector' | Select-Object Path, LineNumber, Line`
  - `git diff --check`
- result:
  - ops docs are consistent with collector runtime v1, Pi node guidance, and the backend collector intake contract
  - the collector Compose group is documented as separate from `infra/local/docker-compose.yml`
  - local-PC development and Ubuntu-on-Pi promotion assumptions are explicit
  - `git diff --check` passed apart from line-ending warnings on the edited Markdown files

## Open Blockers
- `COL-002` handoff is still missing, so source-runner cadence and adapter freeze points may still change
- `BE-005` handoff is still missing, so central intake HTTP path, auth, and batch details remain provisional

## Next Step
- have collector and backend lanes confirm their handoffs so the planned collector env keys and startup sequence can be frozen into actual compose assets later
- route the doc changes through QA or orchestrator review before adding `infra/collector/` files

## Files Touched
- `docs/ops/local-docker-stack.md`
- `docs/ops/deploy-runbook.md`
- `coordination/handoffs/OPS-005.md`
