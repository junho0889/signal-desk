## Task
- id: APP-004
- owner: signal-desk-mobile
- status: done

## What Changed
- implemented cursor-driven next-page loading for Ranking and Alerts using the existing `next_cursor` API contract
- added stale-data context banners to Home, Ranking, Detail, Watchlist, and Alerts using response `generated_at` timestamps
- extended shared mobile abstractions to support pagination without bypassing APP-003 patterns:
  - `LoadableController.replaceData(...)`
  - repository page-merge helpers for keywords and alerts
- updated mock API behavior so cursor pagination can be exercised in mock mode with realistic multi-page payloads
- updated mobile implementation notes with APP-004 behavior, stale thresholds, and verification outcomes

## Current State
- APP-004 code scope is complete in owned files under `app/mobile/` and `docs/mobile/implementation-notes.md`
- Ranking and Alerts now support append-style page loading with loading/retry/end footer states
- stale snapshot context is visible on all core read surfaces

## Verification
- commands:
  - `flutter pub get`
  - `flutter analyze`
  - `flutter test`
  - `flutter run --dart-define=SIGNALDESK_USE_MOCK=true --debug`
  - `Get-ChildItem app/mobile/lib/features -Recurse -Filter *.dart | Select-String -Pattern 'DataFreshnessBanner'`
  - `Get-ChildItem app/mobile/lib/features -Recurse -Filter *.dart | Select-String -Pattern 'Load More|nextCursor|mergeKeywordsPages|mergeAlertsPages'`
- result:
  - all Flutter commands are blocked in this environment with `flutter : The term 'flutter' is not recognized ...`
  - static grep checks confirm stale banner coverage and cursor-pagination wiring on the intended feature screens

## Blockers
- Flutter SDK/toolchain unavailable in this worker environment, so compile/analyze/test/runtime smoke remain pending

## Next Step
- run APP-004 verification on a Flutter-capable machine (`flutter pub get`, `flutter analyze`, `flutter test`, `flutter run`) and hand to QA-003 for regression review

## Files Touched
- `app/mobile/lib/core/network/mock_payloads.dart`
- `app/mobile/lib/core/network/signaldesk_api_client.dart`
- `app/mobile/lib/core/repositories/signaldesk_repository.dart`
- `app/mobile/lib/core/state/loadable_controller.dart`
- `app/mobile/lib/features/shared/data_freshness_banner.dart`
- `app/mobile/lib/features/home/home_screen.dart`
- `app/mobile/lib/features/ranking/ranking_screen.dart`
- `app/mobile/lib/features/detail/keyword_detail_screen.dart`
- `app/mobile/lib/features/watchlist/watchlist_screen.dart`
- `app/mobile/lib/features/alerts/alerts_screen.dart`
- `docs/mobile/implementation-notes.md`
