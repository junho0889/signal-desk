# Dispatch OPS-003

## Task
- id: OPS-003
- owner role: signal-desk-ops
- priority: high

## Objective
Remove the bootstrap loop from the long-running jobs runtime so local and production-like operation can treat migration, demo seed, and recurring alert evaluation as separate responsibilities.

## Required Reads
- `AGENTS.md`
- `coordination/working-agreement.md`
- `.codex/skills/signal-desk-ops/SKILL.md`
- `docs/ops/deploy-runbook.md`
- `docs/ops/local-docker-stack.md`
- `docs/ops/service-model.md`
- `docs/backend/postgres-security.md`
- `docs/backend/implementation-notes.md`
- `coordination/handoffs/OPS-002-QA.md`

## Files You Own
- `infra/local/`
- `docs/ops/deploy-runbook.md`
- `docs/ops/operations-checklist.md`

## Dependencies
- `REL-001` complete or explicitly cleared by orchestrator
- `OPS-002` complete

## Deliverables
- compose/runtime flow that runs migration and demo seed as one-time bootstrap steps
- recurring jobs flow that executes alert evaluation without rerunning bootstrap steps
- updated runbook and checklist covering startup, restart, and recovery behavior

## Verification
- run: `docker compose -f infra/local/docker-compose.yml --env-file infra/local/.env config`, `docker compose -f infra/local/docker-compose.yml --env-file infra/local/.env down`, `docker compose -f infra/local/docker-compose.yml --env-file infra/local/.env up -d --build`, `docker compose -f infra/local/docker-compose.yml --env-file infra/local/.env ps -a jobs-bootstrap`, `docker compose -f infra/local/docker-compose.yml --env-file infra/local/.env logs jobs-bootstrap --tail 100`, `docker compose -f infra/local/docker-compose.yml --env-file infra/local/.env logs jobs --tail 100`, `Invoke-RestMethod "http://127.0.0.1:8000/healthz"`
- expected: bootstrap completes once with `Exited (0)`, recurring jobs continue without repeated migration and seed output, and the API remains healthy

## Checkpoint And Push Requirements
- create a checkpoint commit after any substantial source change, feature addition, or defect fix
- use commit messages that start with `OPS-003`
- push every reviewable checkpoint before pausing unless blocked, then record the exact command and error

## Pause And Resume Requirements
- before pausing, write a handoff in `coordination/handoffs/` with current state, blockers, and next step
- before pausing an unfinished task, update `coordination/resume/OPS-003.md` using `coordination/resume-template.md`

## Handoff
- summarize runtime changes and any operator-visible command changes
- report remaining production-like gaps or config caveats
- report exact verification commands and outcomes
